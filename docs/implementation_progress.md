# Implementation Progress (Macro Areas)

Last review: 2026-08-29

This document is now split by macro area for easier maintenance.

| Macro Area | Status | Details |
| --- | --- | --- |
| Review Findings | Tracked (eighth review: 17 findings - 11 fixed, 6 verified-open; plus 3 found internally, 1 fixed) | [review_findings_tracker.md](implementation_progress/review_findings_tracker.md) |
| Bootloader | Completed (Phase 4 boot-to-CLI verified) | [bootloader.md](implementation_progress/bootloader.md) |
| Kernel Core | **Dead-code removal started**: `kernel/mm/vm.c` and `kernel/core/interrupts.c` are gone - 621 lines, their headers, three syscall handlers and their tests. They were not dead but **built and never consulted**: the live `vibeos_kmain` called `vm_init`, `intc_init` and `intc_bind_timer_irq` on every boot, and `vibeos_intc_dispatch` had no caller outside its own file, so the timer handler that bind registered **could not fire**. Two subsystems in the shape docs/core/ names as this project's most repeated defect. `check-reachable.py` never flagged them and could not: they *were* named outside their files, by kmain.c and syscall.c, both themselves unreached - the question cannot see "named only by code that is itself dead", and the transitive version is now recorded there as its next improvement. Baseline tightened 27 to 12 ([core_dead_code.md](implementation_progress/core_dead_code.md)). **Second increment corrected the method as much as the code**: the claim that `kmain.c` had 17 dead functions was wrong, and wrong by the weak question - the arch layer names only `vibeos_kmain`, but `vibeos_kmain` *calls* the rest. A transitive closure reaches **16 of 18**; that file holds the live kernel CLI, and acting on the claim would have deleted it. Two functions were genuinely unreached, 47 lines: a trap wrapper the arch layer bypasses and an accessor the CLI does not use - the latter checked before removal precisely because `health=` prints every boot. So the weak question errs in **both** directions: it missed two whole subsystems and would have condemned a live interface. **Third increment took C3's decision on syscall.c**: 2282 lines gone - the dispatcher (1502), `syscall_policy.c` (23), `test_syscalls` (687), four capability functions in `user/lib/user_api.c` (52). The decision rule is which choice gives C4 the smaller diff, and the measurement went against integrating: both dispatchers are `switch` statements, and C4 wants a **table** - a syscall id mapped to the checks that apply - so integrating meant carrying 1502 lines of the wrong shape into the change meant to fix the shape. The 23 lines that *were* the right shape are kept as prose in `docs/core/architecture.md`: a design does not have to be compiled to survive. The finding worth more than the lines: chasing the policy layer's callers established that `vibeos_sec_*` appears **zero times in the arch layer** - the kernel that boots has **no capability model at all**, so C4 is not "move the policy layer down" but "there is none on the metal; write one". Two tests went red on the way out and both were the same family: `vibeos_user_api_capabilities()` still reported four features whose implementations had just been deleted - *configured and consulted by nobody* **inverted**, a declaration nothing implements, and a capability flag is read precisely to decide whether a function exists, so the assertion was **inverted rather than removed** and now fails if any comes back non-zero; and one assertion checked the *effect* of a removed function, which is not lost coverage. `process.c` (1142) is **deliberately untouched** - it is the subject of C5, and deleting it in a dead-code commit would decide that phase sideways without the comparison the phase exists to make. **Four external reviews verified finding by finding.** The fourth asked the question the others did not - *what depends on the reverse map being complete?* - and the answer was a property written down nowhere: every consumer cross-checks `rmap_count` against `vibeos_frame_owners`, which is taken by the mapping itself and knows nothing about the node pool, so an under-recorded holder makes the two disagree and the operation **refuses**. Reclaim degrades; nothing is corrupted. That property is in `rmap.h` now and enforced by `check-rmap-crosscheck.py`, validated by sabotage. The check was wrong on its first run - a bare read that only bounds an array - and the fix was a marker requiring prose rather than a wider window, so a second bare use is noticed instead of forbidden. And the branch none of the four reviews found: `rmap_add` fails two ways and only one was counted, so a frame outside the region went unrecorded in silence; it increments `untracked` now. The third titled its table "verified and reachable" and **none of its four findings is reachable**; two of them are word for word defects the core plan has listed for weeks. The second, and three of its four concurrency findings are in code no process reaches: the syscall dispatcher is called only from host tests, `vibeos_sec_audit_record` only from that same dispatcher, and grepping the arch layer for `vibeos_sec_*` returns nothing - the running kernel never calls the security layer at all. Third independent confirmation of C3's subject, and the defect list in that half has grown from three to six, which is evidence for deleting it rather than hardening it. Two findings were real with the wrong mechanism, both worse than stated; one genuine hole the review missed: `rmap_add` returning -1 for a frame outside the pool increments no counter, so that mapping is silently untracked ([external_reviews_verified.md](implementation_progress/external_reviews_verified.md)). **The four unreachable files now say so in a banner**, with the command that verifies it - three reviews in a row read them as live kernel and nothing in the file told them otherwise. And a defect in the checks came out of it: `warnings=` filtered only build-id while `clang-warnings=` also filtered `unused-command-line-argument`, so a clean build always read `warnings=8` - which is why four real `-Wcomment` warnings were dismissed as transient three times in one session. A counter that is never zero is a counter nobody reads; both filters match now and a clean build reads zero. **Yesterday's test debt is paid**: the fork/COW fix now has a deterministic test, and it took three arrangements - the first two asserted the right property about a setup where the defect could not appear, the sixth instance of that shape here. In Progress (ring-3 runtime and shell baseline verified). **C0 started: this kernel now has a performance measurement, the first it has ever had** - three hot paths counted and timed (syscall dispatch, the resolved page fault, the context switch), gated only on being non-zero. Two claims died on contact with the first numbers, which is the point of taking them: the mean is not a baseline (the first boot read 46 million cycles per syscall, because a blocking syscall is timed across the block - it was reporting how long waitpid waited, so a **minimum** was added and reads about 600 cycles), and the counts are **not** deterministic as the plan assumed - six boots spread 4.7% on syscalls, 18% on faults, 21% on switches, so nothing carries a tight ratchet yet and the threshold waits for a characterised spread rather than being written from one sample. A monotone run of five boots looked like drift and got a mechanism written for it; the sixth boot went back up and the mechanism was deleted ([core_perf_baseline.md](implementation_progress/core_perf_baseline.md)). **C1 started**: `check-reachable.py` had been written two days earlier and **never wired into check.sh** - "configured and consulted by nobody", the defect docs/core/ names as this project's most repeated, committed inside the document that names it. It runs now, with a new `check-chokepoints.py`: each security guard declares its call-site count and a move in **either** direction fails. Measuring changed that design - `hw_user_range_ok` has 44 sites because every syscall taking a user pointer validates through it, so the dangerous direction is the count going *down*, a syscall that stopped checking, which reads like a simplification and nothing else would see. It caught its author first: two of five declared numbers were set from a narrower grep than the check walks. Sabotaged both ways, red both ways ([core_c1_checks.md](implementation_progress/core_c1_checks.md)). **C1 finished, and both of its last two checks contradicted the document that asked for them.** `check-blast-radius.py` measures what docs/core/ calls the plan's real progress metric - chosen precisely because line count had been the criterion twice and failed twice - and the plan's own table said a filesystem and a block driver each cost **1** file. Measured: **4, 4, 4, 4, 3, 2** across six extension points, total 21. **Nothing costs 1; the floor is 2.** The four filesystems cited as evidence never used the registry - ext2, ntfs, exfat and iso9660 are named directly in `g_probes[]` with a mount wrapper each and a member in `vibeos_volume_t`, so `vibeos_storage_register` has exactly one caller. And that one caller, FAT, measures 4 as well: somebody still calls the register function, declares it and mounts through it. **A registry whose members need a bring-up call has the same blast radius as no registry at all** - a far sharper target for C6 than "move code out of arch_hw.c", and unsayable before the number existed. The check corrected its author twice on the first run: FAT was hand-counted as 2 by a grep that missed the two symbols which *are* the seam, and a shared symbol prefix turned out not to be a module (`vibeos_x86_64_fat_` matches both the VFS driver and the raw FAT reader, giving 7 for something whose answer is 4). `check-subsystem.py` covers four of the seven module parts across 50 modules and found one already perfect: **`exported_state=0`** - no module's state has a name another file can write, measured with `nm` rather than a regex because the linker's opinion is the only one that counts. Baselines `no_header=15`, `no_case=35`, `state_without_lock=3`. That last property had to be narrowed after reporting **29** on its first run, including `blkdev.c`, which holds its own lock: a detector that flags correct behaviour is one people learn to ignore. Asking instead for the shape that actually cost months - mutable file-scope state and no mention of a lock anywhere - gives three: `vfs.c` (the mount table), `anon.c` (a clock hand), `forkguard.c` (the fork budget). `backing.c` was that exact shape and was right for months on the accident of one caller. Two of the seven parts are deliberately absent with the reason stated: the must-be-zero counter is C2's step 3, which adds counters before the check demanding them, because a check shipped red against thirty modules is the state check-mm-layering.sh sat in for a phase. Both wired into check.sh, both sabotaged and seen red ([core-contract.txt](../scripts/dev/cases/core-contract.txt), [core_c1_blast_radius.md](implementation_progress/core_c1_blast_radius.md)). Four planning documents corrected rather than appended to - README, architecture, invariants and phases all carried the radius-of-1 claim: **the plan named a progress metric, gave it a value, and nothing measured it for the whole time it was the metric.** **C2 started, and its enumeration produced a defect before it produced a list**: 33 report tags against 6 lines carrying `MUSTBEZERO`, of which the gate read **5**. `[GUI] MUSTBEZERO guard_broken=` was printed on every boot for the whole of C0 and asserted by nobody - and it was added by the same person, in the same session, that had just quoted CLAUDE.md's *"a line in the serial log is not a check"*. Knowing the rule and walking into it anyway one session later is the argument for mechanising it. Neither existing check-on-checks could see it, and it fell exactly between them: `check-counters-produced.py` asks whether anything can move a reported counter and the `++` satisfied it; `check-assertions-covered.py` matches every `reason=` against the sabotage cases and `guard_broken` never appeared in one, which is precisely what was missing. A counter produced and never asserted passes both. `check-mustbezero-asserted.py` is that third axis - every `MUSTBEZERO <name>=` literal under `kernel/` against every such pattern the gate searches for - and it reported `unread=1` against the real tree, not a simulation. The gate asserts it now, and the assertion has been seen red on metal: writing one guard word of the back buffer as zero took the boot from `status=pass` to `reason=invariant_failed:gui_guard_broken=1`, counting the single word broken rather than the whole buffer ([gui-guard.txt](../scripts/dev/cases/gui-guard.txt)). **Recorded and deliberately not fixed**: that report line is twelve `serial_puts` calls with no `serial_lock`, which by this project's own rule is twelve critical sections - it has not misread yet only because it runs late, which is an accident of timing and the same sentence already written here about a page cache with one caller ([core_c2_observability.md](implementation_progress/core_c2_observability.md)). **Second C2 finding, and this one had a comment vouching for it**: `hw_cache_audit` compares every resident page-cache page against the bytes its file holds - 1821 pages on the boot it was found - and the gate never read the answer, while the comment above the function said *"Reported as a count, and the boot gate asserts it is zero."* A guarantee documented in the source and provided by nobody is worse than an undocumented gap, because the sentence is what the next reader trusts instead of checking. And the check written an hour earlier for exactly this shape reported ok against it, because **it mechanises a word and not a property**: a counter opts in by spelling `MUSTBEZERO`, and one that never spells it is as unwatched as before. Stated rather than papered over. The harm is the worst class this project has had - once an image page is mapped from the cache rather than copied, one frame is the text of every process running that program. Two assertions added, not one: `cache_audit_changed` zero **and** `cache_audit_checked` non-zero, because an audit that examined nothing reports the healthy answer byte for byte, which is the state VIBEOS_BLK_TIMEOUT was asserted in for months. Sabotaged on metal both ways: forcing one entry to differ gives `exec_cache_page_changed=1` with a witness naming file, offset, phys, byte and both values; returning early gives `exec_cache_audit_checked_nothing`. Removing the word leaves the check green at counters=6 and turns the gate red instead - the check does not fail when a counter leaves its view, it stops counting it, and that asymmetry is recorded rather than smoothed over ([exec-cache-audit.txt](../scripts/dev/cases/exec-cache-audit.txt)). The case file was first written with `ran: yes` on four cases **before any had been run**; running them proved two of the four descriptions wrong, and both are corrected in place. **A sixth external review arrived titled "only verified and reachable problems", and all three of its findings are in `kernel/core/syscall.c` - deleted the same day.** Verified rather than asserted: at the parent commit the arch layer never named `vibeos_syscall_dispatch`, so none of the three was reachable from ring 3; `waitset_initialized` and `kernel_waitset` now have **zero** matches in the tree; `boot_event` survives only as an init and one signal in kmain, reached by no syscall. And H-001's shape cannot exist in the live kernel at all: the real path is `hw_sys_clone_thread`, Linux `clone()`, whose parameters are flags, stack, ptid, ctid and tls - **there is no target process**, the parent is `g_current_task`, so a caller can only create a thread in itself. The principle behind it is right and the live kernel does implement it: every target-pid syscall goes through `hw_signal_permitted`, one definition and four call sites, and `check-chokepoints.py` fails if that count moves in **either** direction. **Fourth consecutive review to land entirely in unreachable code**, and the cause is structural rather than careless - `kernel/core/` reads like the kernel, so every hour spent there is an hour not spent on `arch_hw.c`, where every security defect this project has actually had was found. What still has that shape is `kernel/proc/process.c`: 1142 lines with a process table, thread creation and a capability model nothing consults. A seventh review can still land in it, which is the first argument for taking C5 sooner that came from outside the project. **A sixth review then landed in the live path and was right.** munmap cleared the entry, invalidated *this* core's translation and put the frame back, so a sibling thread on another core could write into a frame the allocator had already reissued - and the source **admitted it**, in a comment saying a shootdown "belongs here too, and is deliberately absent". A synchronous shootdown stays rejected for the measured reason (two runs in twenty-four failed with `tlb_acks below shootdowns`; `syscall` clears IF so a core cannot answer the IPI, and munmap runs far more often than fork). What the comment never considered is the third option the review proposed: **deferred reclamation**, where nobody waits for anybody. The frame is parked and released once every *other* core has loaded CR3 - valid because this kernel enables **no PCID** and marks **no page global** (both checked, both now named in the source, because the argument dies silently if either changes) and because every context switch loads CR3 unconditionally. Same shape as `dead_kstack_base`, already in the tree. **The first version had a hole in exactly the case that matters**: `hw_schedule` returns early when the next task is the current one, so a core running a single thread never reloads CR3 - and a sibling thread spinning on another core is both the thing holding the stale translation and the thing that would never advance. The easy case would have been fixed and the hard case would have degraded to the old behaviour, with no green boot able to tell them apart. Closed by flushing on every core's next tick while anything is parked; the live peak fell from 175 slots to about 45. Ten boots on four vCPUs: ~320 frames deferred each, **released equal to deferred every time**, **overflow zero every time**, `frames_leaked` still zero. `deferred` is asserted non-zero - the race cannot be produced on demand, but "munmap took the safe path at all" can, and a mechanism that silently stopped would leave every boot green with the defect untouched; `overflow` is deliberately not asserted, because falling back is exactly the old behaviour and never worse ([mm_tlb_quarantine.md](implementation_progress/mm_tlb_quarantine.md)). **And the same reviewer then found a second gap, in the mechanism meant to close the first.** `hw_tlb_shootdown` counted a timeout, logged it and **returned** - and every caller has already narrowed a permission or removed a mapping by then, so returning quietly leaves a core writing through a permission that has been revoked, into a page a fork has just shared, with no copy-on-write fault. Worse than stated: **no caller could have checked**, because the backend hook is `void (*shootdown)(uint64_t)` - there is no return value, at any of its five call sites. The fix that closed munmap does **not** transfer, and saying why is the useful part: there the resource in danger was a *frame* and could be parked; here it is a *permission already narrowed*, and the entry must be written before the shootdown is asked for or there is nothing to invalidate - so neither deferral nor rollback is available by the time the timeout is reached. The remaining choices were to wait longer or to stop, and a core that has not answered in two hundred million spins is not about to: it panics naming the reason, which is this project's own position - the difference between a machine that stops and one that says why - and a silent memory-corruption path is strictly worse than a named halt. **Recorded as untested**: the harness cannot make a core miss an IPI for that long, and lowering the bound would collaud a different machine; `tlb_timeouts` is zero on every boot here but has *moved* historically, about three in thirty-two when the shootdown broadcast to every core. The gate already matched the timeout string, which is a detector and not a fix - the operation in that boot had already completed wrongly - and only in CI. **Two MEDIUM findings from the same reviewer fixed, and a HIGH reverted.** `brk` shrinking removed the region and left the page-table entries present and the frames allocated, so memory a program had handed back was still mapped and readable - the region list and the page tables disagreeing, the same shape as the munmap defect. `mmap` had two leaks on its out-of-memory path and only one was obvious: the frame allocated when `hw_map_page` was what failed had no owner to release it, while the pages already mapped were *recoverable* by accident, because `mmap_cur` is not advanced so the next mmap reuses the base and `map_raw` releases what it overwrites - narrower than reported and written down as such. The same two leaks were in `hw_map_user_pages`, which the review did not look at and which `brk` grows through; grepping for the shape turned one fix into two. A worse consequence was hypothesised and **checked and disproved** before being written down. **H-003 (kernel dereferencing a user pointer after a concurrent munmap) is REVERTED and open**, and the diagnosis is worth more than the attempt: the recovery mechanism measured **3/3 green and had never once run**, because its only caller was the futex path where the word is always mapped. A probe reading an unmapped user address from ring 0 on every boot turned that into a `#UD` in one run - recovered `rip=0xaa020` against a kernel linked at `0x4000000`, so `&&label` had produced a **section-relative** offset rather than an absolute address. Not patchable by adding the base; it needs a real exception table with a relocated stub. The probe also had to be moved before it could say anything: run from `kmain` it made the machine go **silent** - 551 lines, no trap, no panic - because kmain runs after every user task has exited and the loaded CR3 can belong to a torn-down address space. The bisection cost an afternoon and the step that should have come first was booting HEAD to confirm it was still green ([mm_brk_mmap_leaks.md](implementation_progress/mm_brk_mmap_leaks.md), [uaccess_recovery_open.md](implementation_progress/uaccess_recovery_open.md)). **C5 was taken before C4, as a step 0 the plan did not have.** Five external findings in two days - the mmap cursor, the region list, signal dispositions, exit_group and descriptors - were one defect: `clone` did `child->proc = parent->proc`, a by-value copy, so everything that belongs to a process was a private copy per thread. The worst of them was in no review: a thread's `mmap` never moved main's cursor, so main was handed **the same base** and its fresh pages silently replaced the thread's. And the sigaction copy loop sat directly under the comment *"Signal dispositions are the process's, so a thread inherits them."* The existing threads test could not see any of it, because every thread was created by main and no worker touched process state. **Tests first, red on the unchanged kernel**: `THREADS_C5_MMAP_FAIL: main was given the thread's base 0x80040d2000`, `THREADS_C5_SIGACTION_FAIL: killed by signal 10`, `THREADS_C5_EXIT_GROUP_FAIL: exited 7, expected 42` - with the gate compiled, the build at rc 0 and both binary and log newer than the source, so a stale image could not fake it. The exit_group stage demands 42 rather than "it ended", because waitpid builds the status from the leader and an exit_group made of SIGKILL alone would report "killed by 9". **The change**: `hw_procstate_t`, referenced not copied - fork and exec create one, a thread takes a reference, the last reference empties the region list before the slot is published free. The mmap cursor is claimed by compare-exchange before mapping, because sharing it turned a deterministic defect into a race and a lock across the mapping would hold thousands of pages with the timer off; brk runs one at a time per process for the same reason. exit_group records the code, SIGKILLs the siblings, and delivery reports the code; exec derives the new process's dispositions *before* giving back the old reference, since that slot can be reissued the instant it is free. Green on the first build, `check.sh all` green, **12/12 boots**. Rewriting mmap for it exposed that **this morning's M-002 fix covered one of two allocation loops** - the reservation branch - and missed the ordinary path malloc reaches, where the leftovers *are* `PTE_USER`; the note claiming ring 3 could not reach them was true only of the loop it fixed. Both unwind now and the document carries the correction. **Recorded as open rather than claimed**, each found after the change was green: no blocking wait in the kernel reads `sig_pending` - futex, pipes, waitpid and console read all re-block after a signal wakes them - so exit_group cannot end a sibling blocked on an unrelated condition, and `kill -9` cannot end a process blocked on a pipe; the Ctrl-C case file had already recorded that delivery to a blocked process has never been observed by any test. `wait4` ignores its options, so WNOHANG blocks. fork snapshots the break without holding it. exec in a threaded process does not end the siblings (H-006, verified, with the sharper consequence that a non-leader's exec'd program is released as a thread and its exit can never reach the parent). **M-004 verified and open**: pipes decide end-of-file outside their lock, and the read half - which the review called optional - is the worse one, reporting EOF with written data still in the buffer ([core_c5_process_state.md](implementation_progress/core_c5_process_state.md), [pipe_eof_race_open.md](implementation_progress/pipe_eof_race_open.md)). **Two more of step 0's open items closed, each red first.** `wait4` never received its options - the dispatcher passed only two arguments - so WNOHANG blocked, which stalls a shell reaping background jobs and made every "bounded" poll in a test unbounded; the red stage was built so the defect could not stall the boot, with a child that exits on its own, and it failed with `waited for the child to exit`. WNOHANG is honoured now; the other Linux wait bits are accepted and ignored, because refusing WUNTRACED would break BusyBox's shell, and unknown bits are refused. And `hw_task_by_tid` was `return hw_task_by_pid(tid);` under a comment saying thread and process ids were the same number - true when written, false since clone gave threads their own ids - so tkill answered ESRCH for a live thread (`pthread_kill on a live thread returned 3`), tgkill could refuse even the leader depending on slot order, and `raise()`, which is `tkill(gettid())`, failed in every thread but the first. Both green on the first build with the BusyBox stages intact, `check.sh all` green, twelve boots: 11 pass, 1 fail ([core_c5_process_state.md](implementation_progress/core_c5_process_state.md)). **One of those twelve boots failed, and was read rather than counted**: `missing:unrecoverable`, a kernel trap whose frame decodes as ASCII file names from the self-test script, then execution at `0x3030303030303030` (`00000000`), with three cores on THREADS.ELF in the four-worker stage - which touches neither wait4 nor tkill. A log from 2026-09-08, three days before any of today's changes, has the same shape in the same stage, so it is recorded as a second, pre-existing intermittent family, separate from the known `CLI_READY` one, mechanism unknown ([boot_repeatability.md](implementation_progress/boot_repeatability.md)). **Then the waits.** No blocking wait in the kernel had ever asked whether a signal was pending - futex, pipe read and write, waitpid and console read all went straight back to sleep when a signal woke them - so exit_group could not end a sibling blocked on anything but the exiting thread's own join, and `kill -9` could not end a process blocked on a pipe. The Ctrl-C case file had already recorded that delivery to a blocked task had never been observed; there had been nothing to observe. The red stage blocks the *leader* on a condition nobody signals, because a blocked worker would let the leader exit 42 and pass on the defect, and it polls with WNOHANG, which is why it waited for that fix: `THREADS_C5_EXIT_GROUP_BLOCKED_FAIL: the process never ended`. Every wait asks `hw_signal_interrupts` now, blocked first and asked second, because `hw_task_set_state` takes no lock and raise wakes without `g_sched_lock`; futex decides woken-or-interrupted under its own lock so a concurrent wake is never lost; and ignored or default-ignore signals interrupt nothing, since the native shell ignores wait4's result. Green, `check.sh all` green, twelve boots: 11 pass, 1 fail. The failure was read, not counted: `FREE_WHILE_MAPPED ... during destroy mappers=0x2 owners=0x1`, field for field the open signature `cow_stale_page.md` has carried as this project's oldest defect, in svc_bomb's fork storm - a path this change does not touch. It carried one thing the original did not, `at_va` at the top page of a native program's stack, which is recorded there as a direction rather than a finding ([cow_stale_page.md](implementation_progress/cow_stale_page.md)). **And a stall no longer hides its own diagnosis.** The blocked-sibling red run reported `missing:CLI_READY` while `THREADS_C5_EXIT_GROUP_BLOCKED_FAIL` sat in its log: a `missing:` reason is raised while the gate drives the CLI, before any invariant runs, and a defect that leaves a process alive stops userland from ever finishing. The gate now appends `stage_failures=` - every `NAME_FAIL` already printed - to a missing-marker reason. Annotation only; the invariants still decide. Deliberately not `FAILED`, which init prints for services that fail by design. Validated rather than asserted: the regex was tried offline first, then the real file's own self-tests ran under the gate's Python, its function found exactly the one failure in the stall log and nothing in four crash logs, and an in-memory sabotage accepting `FAILED` tripped `flags SVC_FAILED`. It has not yet been exercised by a live stall, and says so. **Futex waiters now belong to a process.** Reading `hw_futex_wake` while chasing the four-worker crash showed the waiter table keyed by user virtual address alone - with every Linux program linked at 0x400000 and a forked child sharing its parent's layout, a wake in one process could end a wait in another. It is not claimed as that crash's cause, since musl re-checks after every wakeup. The red stage had a deterministic sign that needed no timing luck: the parent, with no waiter of its own, was told its wake had woken one (`THREADS_C5_FUTEX_XPROC_FAIL: a wake in this process woke 1 waiter(s) in another`). Waiters record their `hw_procstate_t`; the exit path's wake of `clear_child_tid` relies on running before the process reference is given back, and the comment beside it says so, because reversing that would hang every join silently. Green, `check.sh all` green, twelve boots: 10 pass, 2 (both the known four-worker family: the same unhandled write inside THREADS as fail-7 before the fix, shifted by the new stage's 0x320 bytes) fail ([core_c5_process_state.md](implementation_progress/core_c5_process_state.md)). **H-006: exec from a thread now ends the other threads and the caller takes the process id.** The siblings used to keep running the old image and a non-leader kept its tid and thread flag, so the parent could never see the new program exit. After the image loads, the leader is marked a thread under the scheduler lock, siblings get SIGKILL by thread-group id, exec sleeps until they are gone (EINTR and a destroyed uncommitted image if a signal must be acted on), then `pid = tgid`. Exec also clears `clear_child_tid`, which the new image would otherwise have zeroed at exit. Red first (`THREADS_C5_EXEC_FAIL: exited 7, expected 23`), `check.sh all` green, twelve boots: 12 pass, 0 fail. **H-007: a pid resolved to a task slot now still names that task when it is used.** kill, tkill, tgkill, setpgid, getsid and setsid - and the sibling loops of exit_group and exec - found a slot index and used it unlocked, so a slot reaped and refilled by fork in between took the signal or the write. Lookups also matched slots still being built, which keep the previous tenant's ids. Lookup, check and act are now one critical section under `g_sched_lock`, the lock every allocation and every publish to FREE is taken under, and lookups skip RESERVED slots. Two comments claiming the lock could not be held there were wrong. **No red test**: the window is a few instructions between two cores and does not reproduce in a boot; the existing signal stages are the regression. `check.sh all` green, twelve boots: 11 pass, 1 fail - the four-worker THREADS family, same unhandled write at the same instruction (0x405b72 in this build), ending in the free-page-poison panic. **M-006: a length that wraps when rounded to a page is refused.** mmap computed `(len + 0xFFF) / 4096`, zero pages for a length near 2^64, and returned success having mapped nothing, with the base the next call would get; munmap and mprotect had the same wrap in their aligned end, which made munmap drop every region above the address with the pages still mapped. mmap answers ENOMEM, munmap and mprotect EINVAL. Red in the ring-3 ABI self-test first, then split: with only the mmap half fixed the test stayed red, proving the munmap/mprotect checks fail on their own. `check.sh all` green, twelve boots: 11 pass, 1 fail - task_illegal_transition (running->running by hw_task_exit) in THREADS' exit_group stage, a scheduler-exit signature this change does not touch and seen for the first time today; recorded with the exit-window evidence as its likeliest cause, H-007's locked exit_group loop not yet ruled out.**A seventh review scored the project 6.8/10 with a CRITICAL arbitrary-kernel-write, and its author confirmed on request that the paths were archetypes rather than this repository** - nine of its ten cited files do not exist here. Checked anyway, finding by finding: five of seven false against the code (`hw_user_range_ok` has 44 call sites under a chokepoint check; the frame refcount is 16-bit under the layer's own registered lock; `elf.c:242` already does the exact checked addition and range test the review recommends; `hw_spin_lock` begins `pushfq; popq; cli`; IPv4 and UDP both compare declared length against actual). Two were true and both already fixed - the TLB shootdown above, and **the FPU/SSE context switch, which no other review found** and which was real for this kernel's whole life. An archetype aimed at the wrong tree still named where this class of kernel breaks ([review_seventh_verified.md](implementation_progress/review_seventh_verified.md)). **Refactor planned and the plan revised end to end** **Refactor planned and the plan revised end to end** ([docs/core/](core/README.md)): it was a tidying plan built on a line count, and that criterion has been tried twice and failed twice - both times the file grew while the document naming its length as the completion criterion sat unchanged. It is now built on **blast radius**: adding a filesystem or a block driver touches **1** file because those layers are registered into; a character device, an input device, a network interface, a display, a syscall or an ABI all mean editing `arch_hw.c`. Four filesystems and four drivers arrived without their layers being touched, which makes that a controlled experiment rather than a preference. Measured rather than remembered: `arch_hw.c` is **10,087 lines in 19 sections**, of which **7,726 leave** and the largest single block - **4,169 lines, 41%** - is the Linux ABI, which the next refactor on the roadmap has to move anyway, so the file cut and the multi-ABI layer became one phase instead of two. Two findings changed the plan: the scheduler phase is **already done** (`hw_pick_next` consults `vibeos_sched_policy_pick` and falls back to `vibeos_runq_pick`), and observability is far thinner than assumed - **five** must-be-zero counters against 25 report tags - so it earned its own phase ahead of every move. Performance has **never been measured here at all**, so C0 builds a baseline before any seam is added; the boot gate has been recording `phase_history` since it existed and nothing has ever read it. `check-reachable.py` is written and running (`unreached=12 baseline=27`), and the two that matter are the second dispatcher - 2,592 lines the arch layer names zero times, carrying three uncorrected defects, which nothing may reach from ring 3 until they are closed | [kernel_core.md](implementation_progress/kernel_core.md) [core_plan_revision.md](implementation_progress/core_plan_revision.md) [core_perf_baseline.md](implementation_progress/core_perf_baseline.md) [core_c1_checks.md](implementation_progress/core_c1_checks.md) [external_reviews_verified.md](implementation_progress/external_reviews_verified.md) [core_dead_code.md](implementation_progress/core_dead_code.md) [core_c1_blast_radius.md](implementation_progress/core_c1_blast_radius.md) [core_c2_observability.md](implementation_progress/core_c2_observability.md) [review_sixth_verified.md](implementation_progress/review_sixth_verified.md) [mm_tlb_quarantine.md](implementation_progress/mm_tlb_quarantine.md) [review_seventh_verified.md](implementation_progress/review_seventh_verified.md) [mm_brk_mmap_leaks.md](implementation_progress/mm_brk_mmap_leaks.md) [uaccess_recovery_open.md](implementation_progress/uaccess_recovery_open.md) [core_c5_process_state.md](implementation_progress/core_c5_process_state.md) [pipe_eof_race_open.md](implementation_progress/pipe_eof_race_open.md) |
| Process Scheduler | In Progress (classes, nice-weighted fairness, an enforced quantum, a fork-bomb guard; threads reaped where they die) | [process_scheduler.md](implementation_progress/process_scheduler.md)  [sched_quantum_threads.md](implementation_progress/sched_quantum_threads.md)  [sched_torture.md](implementation_progress/sched_torture.md) |
| Boot Repeatability | **24 boots clean out of 24**, from a long-standing 1 in 8. Six defects fixed: three were in the diagnostics rather than the kernel they watched, and three came from CI and nightly logs - a copy-on-write fast path deciding on a fact its compare-exchange could not guard, a kernel address handed to ring 3 as AT_BASE, and a panic that stopped one core instead of the machine, which is what separated every silent wedge from its cause. 24/24 is a good number and not a proof; what is stronger is that two of the three are asserted deterministically and the third turns a silence into a named panic | [boot_repeatability.md](implementation_progress/boot_repeatability.md) |
| Tasks and Scheduling | **S-P0 to S-P6 done**, with two gaps named rather than papered over ([docs/sched/](sched/README.md)). A transition table that refuses illegal state changes, tenancy on every task reference, a tested run queue, lifetime, accounting charged to exactly one place per tick, a policy of classes and nice-weighted fairness and affinity driving the picker - measured over tens of thousands of ticks, every rule confirmed by breaking it - a fork-bomb guard on both fork and clone, and a fork-storm service the gate asserts is stopped and cleaned up after. **Still open**: the quantum is defined and not enforced, so preemption is still whatever the timer does; and thread churn during exec is not exercised | [sched.md](implementation_progress/sched.md) |
| Memory Manager | **Rewrite underway** (ADR-0007). **P0-P3 done; P4 done: the staging buffer is no longer a requirement and has shrunk from 6 MiB to 128 KiB - what held that up turned out to be `vibeos_frame_init` leaving the descriptor flags uninitialised, so the poison detector reported 3019 use-after-frees in a boot with none ([mm_frame_init_flags.md](implementation_progress/mm_frame_init_flags.md)); P6 done (reverse map audited every fork, watermarks and pinning wired, compaction with a fragmentation test); P5 now runs on the machine: an 8 MiB contiguous file on the boot volume gives swap 2048 slots, the boot writes a page through the area and finds it again by reading SWAPFILE.BIN through the filesystem, and the fault handler brings a swapped page back - what is still unproved is the eviction path itself, since `freed_anon` stays zero on a machine nothing runs short of memory ([mm_swap_file.md](implementation_progress/mm_swap_file.md)); P7 done: **the soak is proved** - 26 frames lost at 120 stress rounds and 28 at 12000, so the loss is a fixed cost rather than a per-fork leak. Getting that number honestly meant fixing the machine: the wait for userland to finish treated a blocked task as retired, so the accounting was being sampled mid-fork ([mm_soak.md](implementation_progress/mm_soak.md)). **latency is proved too**: every wait a syscall can reach is bounded, each bound now increments a counter, and the gate asserts them - `VIBEOS_BLK_TIMEOUT` was asserted by the gate and produced by no driver, so that assertion was green and could not go red ([mm_latency.md](implementation_progress/mm_latency.md)). **P7 is done**; what remains for the plan as a whole is exercising the eviction path under real memory pressure**. Physical frames have one owner in `kernel/mm/frame.c`; address spaces record what they own in the page-table entry (`kernel/mm/vmspace.c`), so nothing infers ownership from permission bits any more. No page-table write outside `kernel/mm/`, checked on every build. Host tests, 150 torture seeds and single boots are green; the repeat-boot criterion is not, and closing that comes before P3. Plan in [docs/mm/](mm/README.md); current state in [memory_manager.md](implementation_progress/memory_manager.md) | [memory_manager.md](implementation_progress/memory_manager.md)  [cow_stale_page.md](implementation_progress/cow_stale_page.md)  [mm_rmap.md](implementation_progress/mm_rmap.md)  [mm_reclaim.md](implementation_progress/mm_reclaim.md)  [mm_compaction.md](implementation_progress/mm_compaction.md)  [mm_swap.md](implementation_progress/mm_swap.md)  [mm_acceptance.md](implementation_progress/mm_acceptance.md)  [mm_demand_paging.md](implementation_progress/mm_demand_paging.md)  [mm_frame_init_flags.md](implementation_progress/mm_frame_init_flags.md)  [mm_soak.md](implementation_progress/mm_soak.md)  [mm_latency.md](implementation_progress/mm_latency.md)  [mm_swap_file.md](implementation_progress/mm_swap_file.md)  [lock_deadlock_bound.md](implementation_progress/lock_deadlock_bound.md)  [console_lock_bound.md](implementation_progress/console_lock_bound.md)  [detector_checks.md](implementation_progress/detector_checks.md)  **A failure the gate was naming wrongly, found while chasing the argv one**: three boots in ten failed as `stress_run_did_not_finish` and the stress run had *finished* - 120 rounds, exit 0, machine healthy to the CLI. What went missing was its marker, and all three logs held the same bytes: sixteen NULs then `=120`, where `STRESS_OK rounds` is exactly sixteen characters. A user **stack** buffer whose first sixteen bytes the kernel read as zero. `interleaved_lines` is not at fault - its docstring already states the limit that let this through - so a separate check names the mechanism now ([mm_ring3_write_nul.md](implementation_progress/mm_ring3_write_nul.md)). **Narrowed hard**: the copy-on-write copy is faithful (a fold of the page before and after every resolution, `cow_copy_changed=0`), the page faults several times in the same process rather than once as a comment in the tree claimed, and the defect **needs more than one core** - eight boots at `-smp 1` produce zero corrupted writes against three to five in ten at four. The process migrates, measured on both sides. The explanation that fits every symptom - a stale writable TLB entry surviving a fork that sent no shootdown because the process is single-threaded - is **refuted by one grep**: there is no PTE_GLOBAL and no CR4.PGE in this tree, so every CR3 write flushes. The mechanism is open and the candidate list is written down ([mm_lost_store_smp.md](implementation_progress/mm_lost_store_smp.md)). Four explanations for the **argv** defect were instrumented and closed on the way - the desktop's buffer cannot reach the low user window (`pmm_reserve` truncates the region), the framebuffer is outside managed RAM, a canary shows no overrun, and no frame of it is shared or lost - and the question that file named as its next step was already answered by `double_allocs`, a counter nobody had connected to it. The argv defect **did not reproduce in eighteen boots** against a recorded rate of one in six. **ROOT CAUSE FOUND AND FIXED**: the kernel set CR4.OSFXSR, uses **6955** XMM instructions of its own, and contains **zero** `fxsave`/`fxrstor`/`xsave` - so a user task's SSE registers survive a syscall, an interrupt or a context switch only by luck. clang stores 16-byte stack buffers with `movdqa`, which is why the corruption was **always exactly sixteen bytes** - one XMM register - a width nothing in the memory manager could explain. The zeros are not a fresh page: svc-stress forks a hundred children running a binary that opens with `pxor %xmm0,%xmm0`. And it explains the argv vector too: `COL_TITLETXT` is 0x00FFFFFF, the desktop paints from the kernel on interrupts, and a vectorised fill leaves two white pixels in a lane pair that the resumed task stores into its own buffer - the desktop never touches that memory, which is why every memory counter read zero and was right. Ten hypotheses were measured and killed first, all of them in the memory manager because the symptom looked like memory ([mm_no_fpu_context.md](implementation_progress/mm_no_fpu_context.md)). **Fixed, in two halves.** The context switch saves and restores a 512-byte per-task area (eager, not lazy: CR0.TS needs a fault handler, per-core ownership and cross-core invalidation, three mechanisms on the path this kernel has the most defects in); the initial state is built by hand because MXCSR at offset 24 reads zero as *unmask every SSE exception*; fork copies it, threads get a clean one, and CR4.OSFXSR is set on the boot processor too - only ap_boot.S ever set it, so the BSP had it because UEFI leaves it on. That half alone took the corruption from three boots in ten to **one in twelve**, which is the number that would have been called a fix on a five-boot sweep. The second half is `-mno-sse` on the kernel, as Linux does: a syscall returning to the same process never reaches the scheduler, so nothing saves anything. **22 boots: 0 corrupted writes, 0 xmm lost**, host tests and boot gate green. One near-miss recorded: the first attempt gated the flags on `if(IMAGE AND NOT TESTS)`, and both are on in every real build - it would have compiled to nothing while looking like a fix. **Open**: the argv vector that is not a pointer reads `0x00ffffff00ffffff` - two white pixels, `COL_TITLETXT` from the GUI - and the same value turned up independently as an out-of-range sector in the block layer. The desktop is painting over memory somebody else owns, which is a recurrence of a defect the code comments already describe, and it explains why every memory counter is zero: nothing is freed early, frames are handed out twice ([mm-argv-poison.txt](../scripts/dev/cases/mm-argv-poison.txt)). Two detector defects were fixed on the way: the mapper count was a global two cores overwrote, and the free-side check samples one free in sixteen, so its zero was being read as evidence. |
| Program Loading | **Rewrite underway** ([docs/exec/](exec/README.md)). **X-P0, X-P1, X-P3 and X-P4 done**: every exec refusal names itself and is counted, the segment layout and startup block are host-tested (twelve cases), a failed exec no longer leaks the address space it had already built - eleven refusal points route through one unwinding path - and the interpreter substitution is one function the build checks for. **X-P2 mapping on**: image pages come from the page cache, 4242 mapped against 237 copied. It took four attempts; the cause was that `kernel/mm/backing.c` had **no lock** - invisible while the cache had one caller under the exec lock, and a corruption the moment the loader became a second one. Fourth layer in this project to need a `set_lock`. The staging buffers and demand paging are still open | [exec.md](implementation_progress/exec.md)  [exec_silent_failure.md](implementation_progress/exec_silent_failure.md) |
| Splitting arch_hw.c | **Two cuts done**: 9088 to 8359 lines. The Linux socket syscalls and signal delivery moved to their own files behind a declared seam (arch_hw_internal.h). The file had grown 633 lines during a session spent reducing it, so this plan carries the line count as a completion condition rather than a side effect | [arch_extraction.md](implementation_progress/arch_extraction.md) |
| Interrupt Handling | In Progress (CPU traps, PIT and APIC timer paths verified) | [interrupt_handling.md](implementation_progress/interrupt_handling.md) |
| System Call Interface | In Progress (Linux process semantics verified from ring 3) | [system_call_interface.md](implementation_progress/system_call_interface.md) |
| IPC Subsystem | In Progress | [ipc_subsystem.md](implementation_progress/ipc_subsystem.md) |
| Driver Framework | In Progress (virtio-blk **and AHCI/SATA**, virtio-net, PS/2 keyboard and mouse, framebuffer desktop verified; both disk controllers gated in CI). **virtio-net transmit is serialised now**: it had one staging buffer, one descriptor and a plain 16-bit `avail->idx++` shared by every core - the same defect virtio-blk had, never applied here. It presented as a boot wedge with a core spinning on a completion another core had already taken, and QEMU named it on its own stderr: index 65535 available | [driver_framework.md](implementation_progress/driver_framework.md) |
| Filesystem Layer | **Refactor underway** ([docs/io/](io/README.md)). **I0-I5 and I4b, I4c done; I5b done; I5c closed on its terms (the journal is over-determined against single faults, so a fourth torture would need two at once - written up rather than built); **I6 done for virtio-blk** - the queue and the completion contract exist and are sabotage-checked, and the disk now wakes the waiting core instead of being polled: about twenty thousand interrupts a boot ending some sixty per cent of the waits, asserted by the gate. **AHCI too**, taken second as a port rather than an investigation once the path was proved - and it turned out to have the same two defects, Interrupt Disable left as the firmware set it and the port interrupt-enable register never written. **I6 is done**; **I7 nearly done** - a disk that fails on demand, swept across which request fails and how, with the block layer three promises checked together (told it failed, a reason, nothing in flight), plus a transfer that fails after moving half the data, a barrier the device refuses, and the layer re-entered while a request is in flight. What stays open is genuine concurrency: two cores inside the layer at once needs the boot, not a host test.** A boot mounts six volumes across five filesystem implementations - two FAT, plus ext2, ISO9660, NTFS and exFAT read from images built by the host own mkfs tools. Every sector goes through one block cache; the block layer bounds every request and gives a failure a reason; writes are proved within a boot against an offset-carrying pattern; the disk reports its own partition table, and can now write one, MBR or GPT. The kernel has a log on a second disk that survives a reset. Four of the five filesystem drivers had never parsed a byte before this phase, and three of them were broken in ways only a real image could show - which is what the phase was for. **The AHCI job of the Filesystem Runtime Gate had been red for days and is green now**: "the disk" meant whichever driver bound first, which is the boot disk only when there is one disk - I5b added a second, deliberately on the other controller, and with the ESP on AHCI adapter 0 is a blank 4 MiB file. The boot did **one read, one sector**, every exec said not-found, every task exited 127, and every must-be-zero counter in the block layer read zero and was right: the read succeeded, it just was not a filesystem. A machine that reads the wrong disk perfectly has nothing to report. The same false invariant was encoded twice, one file apart - the log sink hardcoded adapter 1 as "not the boot disk" and so wrote over the ESP. The boot disk is now whichever carries a mountable volume, both controllers pass, and the sabotage reproduces the CI signature exactly ([io_boot_disk.md](implementation_progress/io_boot_disk.md)). **Open**: nothing yet checks that a write survives a reboot, and the wedge rate rose with the last two images - catching one found a core executing the free-page poison on a frame that is another core CR3, which is a memory-lifetime defect rather than a filesystem one. | [filesystem_layer.md](implementation_progress/filesystem_layer.md) [io_blkdev.md](implementation_progress/io_blkdev.md) [io_read_contract.md](implementation_progress/io_read_contract.md) [io_one_cache.md](implementation_progress/io_one_cache.md) [io_writes_proved.md](implementation_progress/io_writes_proved.md) [io_volumes.md](implementation_progress/io_volumes.md) [io_mounts.md](implementation_progress/io_mounts.md) [io_parttab.md](implementation_progress/io_parttab.md) [io_gpt.md](implementation_progress/io_gpt.md) [io_ext2.md](implementation_progress/io_ext2.md) [io_iso9660.md](implementation_progress/io_iso9660.md) [io_ntfs_exfat.md](implementation_progress/io_ntfs_exfat.md) [io_logsink.md](implementation_progress/io_logsink.md) [io_boot_disk.md](implementation_progress/io_boot_disk.md) | 
| Networking Stack | In Progress (IPv4 runtime plus portable route/firewall data-path enforcement verified). **M-005: a TCP RST is believed only when it could have come from the peer** - it closed any connection matching address and ports, and a listening socket too; now RFC 5961 3.2 (exact rcv_nxt resets, in-window gets a challenge ACK, outside is dropped; SYN_SENT needs the RST to acknowledge our SYN; LISTEN ignores it). Red host test first, twelve boots: 11 pass, 1 fail - task_illegal_transition (running->running by hw_task_exit) in THREADS' exit_group stage, a scheduler-exit signature this change does not touch and seen for the first time today; recorded with the exit-window evidence as its likeliest cause, H-007's locked exit_group loop not yet ruled out | [networking_stack.md](implementation_progress/networking_stack.md) |
| User Space Interface | In Progress (unmodified static Linux binaries verified from ring 3) | [user_space_interface.md](implementation_progress/user_space_interface.md) |
| Diagnostics & Observability | In Progress (six runtime detectors, all gated; they closed the premature-free defect that had been chased three times from the far end) | [diagnostics.md](implementation_progress/diagnostics.md) |
| Init System | In Progress (native ring-3 init is PID 1 and supervises a four-service manifest in the guest: start, clean stop, bounded restart, and a real crash the kernel survives) | [init_system.md](implementation_progress/init_system.md) |
| Linux Compatibility | In Progress (static, position-independent **and dynamically linked** binaries all run end to end and are gated) | [linux_compatibility.md](implementation_progress/linux_compatibility.md) |
| Windows Compatibility | Not started (a two-entry translation table, no runtime) | [windows_compatibility.md](implementation_progress/windows_compatibility.md) |
| macOS Compatibility | Not started (same, and deliberately dormant) | [macos_compatibility.md](implementation_progress/macos_compatibility.md) |

## Notes

- Status reflects code currently present in repository, not only planned milestones.
- A file existing under `user/compat/` is not progress. The Windows and macOS
  translators are twenty-four lines each, know two syscall numbers, and nothing
  calls them; they are tracked as not started for that reason. The Linux
  compatibility that works is the kernel syscall layer, which is a different
  piece of code entirely.
- "Verified" here means a gate fails if it regresses, unless the entry says otherwise. The framebuffer desktop is the standing exception: its serial-visible state is gated, its pixels are checked by hand with `scripts/dev/screenshot.py`.
- Each macro-area file contains:
  - implemented items verified in code
  - pending items
  - next execution checkpoint
