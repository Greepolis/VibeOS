# Review findings tracker

One place for every external-review finding and its state, so none is lost
between sessions. Updated whenever a finding arrives, is verified, or closes.

**Read the key first.** Reviewers number from 001 in every review, so `H-001`
names different defects in different files. A finding is identified here by
**review + ID**, and the IDs below are the reviewer's own, even where they collide.

Status words: **fixed** (commit named, red test first unless the row says why
not), **in progress**, **verified-open** (confirmed against the code, not yet
fixed), **false** (checked and refuted - evidence linked).

Earlier reviews are closed and written up: the sixth
([review_sixth_verified.md](review_sixth_verified.md)), the seventh
([review_seventh_verified.md](review_seventh_verified.md)) and those before
([external_reviews_verified.md](external_reviews_verified.md)).

## Eighth review (2026-09-10 onward, arriving one finding at a time)

| ID | Sev | Finding | Status | Evidence / next step |
|---|---|---|---|---|
| M-001 | M | brk shrink never unmapped | fixed `1dc5b67` | [mm_brk_mmap_leaks.md](mm_brk_mmap_leaks.md) |
| M-002 | M | mmap failure left pages mapped (both loops) | fixed `1dc5b67` + C5 | second loop found later; same file |
| H-003 | H | futex validates a user pointer, then reads it after a spinning lock | fixed (commit "core: a user copy that faults returns an error instead of panicking") | assembly copy with a recovery point in the trap handler; futex read and the exit join-word write use it. [uaccess_recovery_open.md](uaccess_recovery_open.md) |
| H-004 | H | two region-list heads into one pool across threads | fixed `ffa8463` | [core_c5_process_state.md](core_c5_process_state.md) |
| H-005 | H | exit_group ended only the calling thread | fixed `ffa8463` | same file |
| M-003 (a) | M | signal dispositions copied per thread | fixed `ffa8463` | same file |
| M-003 (b) | M | pipe write tests `readers` outside `g_pipe_lock`; read half worse | fixed (commit "core: a pipe decides end of file under the lock that holds its data") | both decisions moved into the critical section with the data they depend on. **No red test**: the windows are a few instructions between two cores. [pipe_eof_race_open.md](pipe_eof_race_open.md) |
| H-006 | H | exec from a thread left siblings running and the wrong id | fixed `8a3903c` | THREADS_C5_EXEC, red first |
| M-004 | M | tkill/tgkill could not reach non-leader threads | fixed `d6d6b1b` | reported again later; already closed |
| H-007 | H | pid resolved to a slot index, used unlocked (ABA); lookups matched slots being built | fixed `da50ce6` | **no red test**: the window does not reproduce in a boot; stated in the commit |
| M-005 | M | TCP RST accepted without a sequence check; also closed listeners | fixed `b4a65ad` | RFC 5961 3.2; host test red first |
| M-006 | M | mmap `len + 0xFFF` wraps to zero pages; munmap/mprotect aligned end wraps | fixed `a40aab0` | red in the ABI self-test, split-verified |
| H-008 | H | TCP ISN = clock (`0x1000 + now_ms`, listen `0x2000 + now_ms`) | fixed (commit "net: TCP initial sequence numbers come from a secret") | RFC 6528 with SipHash over one stack secret; host test red first. **The secret is weak under QEMU TCG**: no RDRAND, so it comes from the TSC mix and the boot log says so. Real entropy is its own open item |
| H-009 | H | DHCP: predictable xid; ACK not bound to the chosen server, offer or chaddr; renewals from anyone | fixed (commit "net: a DHCP reply belongs to this client's transaction with its server") | xid from the stack secret; server port, chaddr, server id; ACK only from the chosen server for the offered address, renewals for the address in use; **a NAK from anyone used to drop the lease** and now must come from the server. Red twice (as written, and with the xid check off). Not closable by a client: an on-segment attacker answering the broadcast DISCOVER first |
| M-007 | M | DNS: predictable id, fixed source port 0xC353, no server/port/question check | fixed (commit "net: a DNS answer is believed only if it answers the query sent") | server, port 53, response bit, id and the single question checked; id and port from the stack secret per query. Host test red first - **the first version was red for the wrong reason** (it captured an ARP request) and was corrected and re-proved red at the right step |
| H-010 | H | read() on a pipe, recv(), recvfrom() validate the buffer, block, then write it after wake-up; a sibling's munmap makes it a ring-0 fault -> panic | fixed (commit "core: a user copy that faults returns an error instead of panicking") | `hw_pipe_read` (write inside the blocking loop), `hw_net_recv`, `hw_sys_recvfrom`; **also the console read** (same shape, not in the report). Fixed with H-003, same commit: pipe read/write, console read, recv/recvfrom/send/sendto through the fault-tolerant copy (socket data via a kernel bounce buffer). Boot probe asserted by the gate; sabotage of the recovery branch turns it red |
| M-008 | M | any ARP overwrites the cache entry, gateway included | fixed (commit "net: a known neighbour's hardware address changes only by a reply the stack asked for") | an entry changes only through a reply to the request the stack sent; a request for this host may add, never change; stale entries are used and refreshed by a request. Host test red first. ARP has no authentication: racing the real reply to a request is still possible |
| M-009 | M | exFAT contiguous file: `first + index` wraps in 32 bits to a valid cluster; `first_cluster` from the entry never range-checked | fixed (commit "fs: cluster, run and length arithmetic from a volume cannot wrap") | `exfat_nth_cluster` (`return first + index`), parse takes `rd32(stream + 20)` as is, `exfat_read_cluster` checks only the wrapped value. **Also, not in the report:** `read_at` truncates `(offset + done) / cluster_bytes` to 32 bits, so a large declared size wraps the index even with a valid first cluster. Host exFAT tests exist, so the red test is deterministic |
| audit | M | exFAT, ext2, ISO9660 and NTFS `read_at`: `offset + len > node->size` after `offset < size` | fixed (commit "fs: cluster, run and length arithmetic from a volume cannot wrap") | found by the H-011 audit. Reachable only with a size declared near 2^64 and an offset within 4 GiB of it; then `len` is not trimmed and the read runs past the declared end (in exFAT, straight into M-009's index truncation). Fix with M-009/M-010: `len > size - offset` |
| M-010 | M | NTFS run-list: `len_size`/`off_size` up to 15 bytes, shifted `<< (8 * i)` into 64-bit types - undefined behaviour | fixed (commit "fs: cluster, run and length arithmetic from a volume cannot wrap") | `ntfs_next_run` checks only that the bytes fit. **More than reported:** the sign extension `-((int64_t)1 << (8 * off_size))` is already undefined at `off_size == 8`, the largest *legal* size; shifting a byte into the sign bit of `int64_t` is undefined too. The reviewer's second candidate is also real: `vcn < seen + run.length` overflows on a crafted length |
| H-011 | H | NTFS `off + attr_len > size` wraps in 32 bits, so a huge resident `$DATA` value is accepted and `read_at` copies past `rec[]` - **kernel stack contents to user space** | fixed (commit "fs: an NTFS attribute length cannot open its record up") | bounds are `len > size - off` after `off <= size` in `ntfs_find_attr` and `ntfs_data_attr`; host test red first. **Audit of every parser done:** the 8/16-bit record walks (ext2, ISO9660, NTFS index and run headers) cannot wrap; the four read paths below share a weaker form of the same pattern, recorded as its own row |
| H-012 | H | FAT: a first cluster from a directory entry is never range-checked; `fat_cluster_lba` is 32-bit and nothing bounds a sector by the partition - reads of other partitions' sectors | fixed (commit "fs: a FAT cluster from the disk names a sector of its own partition") - **worse than reported, and the write half fixed too** | `fat_cluster_lba` (`data_lba + (cluster - 2) * spc`, uint32) is checked nowhere; only `fat_next_cluster` checks `max_clusters`. The partition's total sectors is a local of the mount, never stored. **Writes too:** a parent directory's cluster comes from `fat_resolve` unchecked, `fat_dir_sector` turns it into a sector with `fat_cluster_lba`, and create, unlink and mkdir *rewrite* that sector with one 32-byte entry changed - a crafted subdirectory turns "create a file here" into a write to a sector of another partition. Data writes use freshly allocated clusters and are not affected. Fixed in `vibeos_fat_cluster_sector` (host-tested, red first): cluster range, 64-bit sector, partition bound; `fat_cluster_lba` returns 0 for a refused cluster and every read, list, directory-sector and write caller stops on it. Twelve boots 12/12 |
| H-013 | H | ext2: inode block pointers never checked against the volume; `blocks_count` not kept after mount, so a crafted pointer reads sectors past the partition | fixed (commit "fs: an ext2 block number names a block of its own volume") | `ext2_read_block` computed `part_lba + block * sectors` with no bound; `ext2_map_block` takes direct and indirect pointers as they come. **Also, not in the report:** `ext2_read_inode` takes the inode table block from the group descriptor unchecked, so a lookup alone can read past the volume. Fixed in the same place: the mount keeps `blocks_count` and `ext2_read_block` refuses any block at or past it. Host test red first, with a volume declared smaller than its device - an image whose volume fills the device would fail at the device and pass for the wrong reason |
| H-014 | H | TLB shootdown timeout returns and the caller proceeds, so a permission restriction "succeeds" while a target core may still hold the old translation | **already fixed - the finding read stale code** (`41fd9af`) | the finding quotes the timeout body (`store 0; tlb_timeouts++; hw_log(...)`) and stops one line early: it is immediately followed by `hw_panic("TLB shootdown timed out: a core still holds a translation the caller has already revoked")`. There is one shootdown implementation (`hw_tlb_shootdown`, the `g_be.shootdown` hook), and on timeout it fail-stops - exactly the finding's recommended fix. An earlier review found the old "return and continue" behaviour; the comment there records the whole reasoning. No live defect |
| H-015 | H | TLB quarantine overflow (>512 parked frames) falls back to an immediate `vibeos_frame_put` - the old racy release - so a frame can be recycled while another core holds a stale TLB entry; a single process can force it by munmapping >512 not-yet-quiescent pages | fixed (leak on overflow; dynamic quarantine is the follow-up) | `hw_tlbq_put`: the fallback is labelled "status quo: the old, racy release". The boot gate asserts `tlbq_overflow=0` so CI catches it, but that is not a runtime guarantee. Fix: on overflow do a synchronous shootdown for that frame's address space then put, or apply backpressure until a slot is quiescent - not the racy put |
| H-016 | H | `rt_sigaction` and `rt_sigprocmask` dereference the user pointer directly after `hw_user_range_ok` (old/act read+write); a sibling thread's munmap between check and access faults in ring 0 | fixed (`298b68d`) | same class as H-003/H-010 but thread-race, no blocking. `hw_sys_rt_sigaction` (old_uptr writes, act reads), `hw_sys_rt_sigprocmask` (old_uptr write, set read). Fix: `vibeos_uaccess_copy` |
| H-017 | H | `rt_sigaction` stores the handler (`act[0]`) as `sig_handler[sig]` with no canonicality/user check; `hw_signal_deliver` puts it in `frame->rip`, and iretq to a non-canonical rip #GPs in ring 0 -> kernel panic, from ring 3 via rt_sigaction + kill | fixed (`05fa793`, red test) | canonical rip stays canonical, so validate at install: `hw_user_addr_ok(handler)` (rejects non-canonical and wrong-window). A canonical-but-unmapped handler faults in ring 3, which is safe. Red test possible: non-canonical handler + signal panics today |
| H-018 | H | `rt_sigreturn` reads the sigframe with a direct deref after `hw_user_range_ok`, and restores `rip`/`rsp` from the user-controlled frame without a canonicality check -> iretq #GP in ring 0 | fixed (read `298b68d`, rip `05fa793`) | cs/ss/rflags ARE normalised (no privilege escalation), so the live defect is the non-canonical rip/rsp -> iretq fault, plus the TOCTOU read. Fix: copy the frame via `vibeos_uaccess_copy`, validate `restored.rip`/`rsp` canonical+user, SIGSEGV otherwise |
| H-019 | H | `hw_copy_user_string` validates each byte then derefs it directly; a sibling munmap between the two faults in ring 0. Used by execve, open, unlink, mkdir, stat/readlink | fixed (`298b68d`) | `int hw_copy_user_string(uptr,dst,max)`: `hw_user_range_ok(uptr+i,1,0)` then `dst[i]=*(uptr+i)`. Fix: fault-safe per-byte `vibeos_uaccess_copy`, keep `hw_user_range_ok` as the address-policy check |
| H-020 | H | `readv`/`writev` validate the whole iovec array once, then deref `v->len`/`v->base` per element; a sibling munmap of the array page faults in ring 0 | fixed (`298b68d`) | `hw_sys_writev`/`hw_sys_readv`. Fix: copy each `hw_iovec_t` via `vibeos_uaccess_copy` into a kernel struct before reading base/len |
| M-017 | M | signal 64 is accepted (`sig >= VIBEOS_HW_NSIG` where NSIG=65) but `1ull << sig` with sig=64 is undefined; the pending/blocked bitmask is 64-bit keyed by signal number, so signal 64 has no representable bit and can stay pending undelivered | **fixed** (`278a803`) | `VIBEOS_HW_SIG_MAX 63u` added; the five range checks that read `sig >= VIBEOS_HW_NSIG` now read `sig > VIBEOS_HW_SIG_MAX`, so a signal that has no representable bit is refused rather than accepted and lost. Red test: `syscall(SYS_kill, getpid(), 64)` expects EINVAL, gated in musl_signal.c (SIG_OK) |
| H-021 | H | clone() writes CLONE_PARENT_SETTID/CLONE_CHILD_SETTID through a user pointer after the range check; a sibling munmap faults in ring 0 | fixed (`188ec0f`) | `hw_sys_clone_thread` ptid/ctid stores. Same validate-then-use class; fix with `vibeos_uaccess_copy`, dropped on fault (the thread is created either way) |
| H-022 | H | waitpid() writes status_ptr directly after publishing the task FREE, releasing the lock and re-enabling interrupts; a sibling munmap between check and store faults in ring 0 | fixed (`188ec0f`) | `hw_sys_waitpid`. The reap is already complete, so a faulted write is dropped and the pid still returned, as Linux does after EFAULT there |
| H-023 | H | signal delivery builds the sigframe directly on the user stack after the range check (magic/blocked/frame and the return address); a sibling munmap faults in ring 0 | fixed | `hw_signal_deliver`. Built in a kernel buffer and copied out via `vibeos_uaccess_copy`; on fault no partial frame, task takes SIGSEGV |
| H-024 | H | arch_prctl(ARCH_GET_FS) writes `t->fs_base` to the user pointer after the range check | fixed | `hw_sys_arch_prctl`. `vibeos_uaccess_copy` |
| H-025 | H | ioctl TIOCGPGRP/TIOCSPGRP deref the user pointer directly after the check (write pgid, read pgid) | fixed | `hw_sys_ioctl`. `vibeos_uaccess_copy` both directions |
| H-026 | H | clock_gettime writes the timespec (`ts[0]`, `ts[1]`) through a user pointer after the range check; a sibling munmap faults in ring 0 | fixed (with a proactive sweep of execve argv/poison, time, crash stack). Still direct: pipe2 fd write, hw_stat_wr (all stat paths) - needs a kernel-buffer stat + undo; follow-up | | `hw_sys_clock_gettime`. Same validate-then-use class; fault-safe copy. NB: array-indexed (`ts[i]=`), which the narrow `*(...)(uintptr_t)` grep missed - the class is broader than that pattern, a proper sweep must include `ptr[i]` user derefs |
| H-027 | H | setsid() checks and assigns current->sid/pgid OUTSIDE g_sched_lock, then takes the lock only for the sibling-member updates; setpgid/getsid/kill read these under the lock, so a sibling races the transition | **fixed** | `hw_sys_setsid`: the `pgid==tgid` check and both current-> assignments are now under g_sched_lock with the sibling loop, so check+modify is one linearizable step; returns current->tgid to avoid a post-unlock read of a mutable field. No red test (thread-race, H-007 precedent); full check green, 3 boots pass |
| H-028 | H | TCP SYN_RECEIVED stores cs->parent = listener slot index; if the listener closes and the slot is reused, the final ACK delivers the child into the new socket backlog - no identity check | **fixed** | Each socket gets a monotonic `gen` at sock_alloc (net->sock_gen_seq); the child records the listener's `gen` as `parent_gen` at SYN time, and the final-ACK backlog insert now requires the parent slot to still be `used && type==TCP && state==LISTEN && gen==parent_gen`, else the child is dropped. Red test `test_inet_tcp_accept_aba` (close listener, new listener reuses the slot, drive the final ACK; the stranger's backlog must stay empty): fails on the `used`-only code, passes with the fix. Full check green, 3 boots pass |
| H-029 | H | mounted filesystems are not bounded by their partition: storage_scan drops sector_count, so ISO9660/exFAT read image-declared offsets/clusters as device LBAs and can read a neighbour partition | fixed (exFAT+ISO, red test; FAT/ext2 authoritative-bound upgrade is a follow-up) | root cause in `storage.c` (only first_lba kept). Fix: propagate sector_count, bound each read by [first_lba, first_lba+sector_count). NB my H-012/H-013 (FAT/ext2) bounded by the IMAGE-declared size, not the partition - not fully authoritative; upgrade them too (follow-up) |
| M-018 | M | newfstatat truncates node.size (64-bit) to uint32_t, so st_size is wrong (mod 2^32) for files > 4 GiB | fixed (no red: fstat via fd path still 32-bit, separate) | `hw_sys_newfstatat` uint32_t size -> hw_write_stat. Keep it uint64_t. Deterministic test (a node > 4 GiB) |
| M-019 | M | ISO9660 directory sector count `(length + 2047) / 2048` wraps in uint32 (length near UINT32_MAX -> sectors 0), so a directory reads as empty and its files vanish | fixed (red test) | `iso_dir_find` and `iso_op_list` (duplicated). Use `((uint64_t)length + 2047) / 2048`, centralize, and bound by the volume |
| M-020 | M | blocking socket syscalls (connect/accept/recvfrom) hold a raw `hw_fd_t *` across the wait loop and re-read `f->net_sock`; a sibling thread closing+reusing the FD slot makes the call act on a different socket - slot-index ABA in the FD table | fixed (reopened 2026-09-24 and closed: recvfrom had been missed) | `hw_sock_stable(f, sock, gen)` in linux_socket.c: capture socket index + the socket's `gen` before blocking, re-verify on every wake (fd still used, net_sock unchanged, socket gen unchanged), else EBADF. Counted at `[NET] MUSTBEZERO sock_fd_aba` (also `sock_stale_parent`, H-028) - the network module's C2 must-be-zero. **Reopened by an external review**: the fix reached connect, accept and the stream read (`hw_net_recv`) but not `hw_sys_recvfrom`, the datagram syscall, which still re-read `f->net_sock` on every pass of its wait while this row said fixed. It re-verifies now, and `check-net-stable.py` (in `check.sh`) makes the rule mechanical: every function in net.c that holds an `hw_fd_t` and calls `hw_net_wait_tick()` must call `hw_sock_stable`. `net-sock-stable.txt`: removing it from recvfrom or accept is red by name |
| M-022 | M | ext2 directory size can wrap and hide the whole directory. inode_size() builds a 64-bit size from i_size+i_dir_acl; ext2_dir_find() then does nblocks=(size+block_size-1)/block_size, which overflows to 0 for a size near UINT64_MAX -> no blocks scanned, every lookup fails. Same in ext2_op_list(). Value comes straight from the image; distinct from M-019 (that was ISO9660) | fixed | kernel/fs/ext2.c: inode_size() ~202-209; ext2_dir_find() nblocks ~219; ext2_op_list() ~378-380. Fix: validate dir size vs blocks_count*block_size at inode read; compute blocks without overflowing sum (size/block_size + (size%block_size!=0)); don't treat i_dir_acl as high-32 unless the feature level allows | **Fix:** `ext2_dir_nblocks()` divides without the overflowing sum and clamps to blocks_count; red test `test_ext2_dir_size_wrap`.
| M-023 | M | orphaned TCP child stays allocated forever when the listener is closed during the handshake. H-028's parent check (used/type/state/gen) correctly drops the child, but the else path never closes/frees it: it still sets ESTABLISHED and clears rto_deadline_ms, then just skips the backlog insert. Child was sock_alloc'd (owner_pid=0, no FD), and the reclaim timer only frees sockets with close_deadline_ms!=0 -> used=1 indefinitely; a later RST -> CLOSED but not freed. Repeatable -> socket table exhaustion. Distinct from H-028 (that was misdelivery) | fixed | kernel/net/inet.c: tcp_input() child->ESTABLISHED ~1602-1605, parent-fail branch ends with no cleanup ~1614-1619; sock_alloc() ~481-496; vibeos_inet_poll reclaim ~1848-1855; tcp_input_rst ~1464-1474. Fix: if parent fails validation, do NOT establish - close+free the child immediately (used=0, CLOSED, deadline 0); centralize tcp_abort_socket(). Add a red test (SYN->close listener->final ACK->slot freed, no table exhaustion) | **Fix:** the final ACK validates the parent BEFORE establishing; on failure `tcp_abort_socket()` frees the child. Red test `test_inet_tcp_orphan_child_freed`.
| H-030 | H | TCP half-open connections not reclaimed after retry exhaustion (remote DoS). Each SYN to a listener creates a SYN_RECEIVED child via sock_alloc(); when retries hit TCP_MAX_RETRIES the timer only sets state=CLOSED, reset=1, rto_deadline_ms=0 - never used=0 nor close_deadline_ms. Slot stays occupied. A remote attacker sends SYNs without completing the handshake, repeats to exhaust VIBEOS_INET_MAX_SOCKETS, blocking new TCP. No FD or listener-close needed (child is kernel-owned until accept). Distinct from M-023 | fixed | kernel/net/inet.c: child creation tcp_input() ~1564-1586; retry reclaim vibeos_inet_poll() ~1878-1900 (~1886-1890 sets CLOSED but doesn't free); sock_alloc reuses only when used==0 ~480-496; general reclaim needs close_deadline_ms!=0 ~1848-1855. Fix: on retry exhaustion abort+reclaim fully (used=0, CLOSED, timer 0, remove backlog refs); centralize tcp_abort_socket(). Test: MAX_SOCKETS incomplete SYNs, wait retry expiry, table returns | **Fix:** retry exhaustion of an unowned SYN_RECEIVED child calls `tcp_abort_socket()` (also drops backlog refs). Red test `test_inet_tcp_half_open_reclaimed`.
| M-031 | M | accepted TCP socket never inherits process ownership, leaks at exit. The child from the listener has owner_pid=0 (sock_alloc zeroes it); accept() assigns it to the new FD but never calls vibeos_inet_socket_set_owner(). hw_task_exit() releases sockets by tgid via vibeos_inet_release_owner_sockets(), which misses this child (no owner); the FD cleanup only sets f->net_sock=-1, not closing the stack socket -> orphaned kernel resource until table exhaustion. Normal path: listener->accept->thread exit without close(). Distinct from M-021 | fixed | kernel/arch/x86_64/linux_socket.c: hw_sys_accept() t->fds[nfd].net_sock=child ~195 without ownership; kernel/net/inet.c sock_alloc ~481-496; arch_hw.c hw_task_exit ~5200-5221. Fix: after allocating the FD in accept(), set child owner_pid=t->tgid under g_net_lock. Test: open listener, accept many, exit without close(), all TCP slots reclaimed, later socket() no ENOBUFS | **Fix:** accept() sets the child owner to t->tgid under g_net_lock. (Covered by the boot gate; no host harness for the syscall layer.)
| M-032 | M | recvfrom() consumes the datagram and accept() consumes the connection even if copying the peer address to user space fails. Both do the socket op first then hw_write_sockaddr(); the return of hw_write_sockaddr() is ignored, so an invalid addr_uptr loses the connection/datagram with no -EFAULT. Reachable by passing a bad pointer; no SMP race. Distinct from H-016/H-019/H-026 (here the caller ignores the copy failure and keeps the side effect) | fixed | kernel/arch/x86_64/linux_socket.c: hw_sys_accept() (void)hw_write_sockaddr ~204; hw_sys_recvfrom() ~417-419; hw_write_sockaddr returns -1 on bad ptr ~35-57. Fix: on copy failure propagate -EFAULT; for recvfrom prepare the datagram before removing it from the socket (or make semantics explicit); for accept handle the failure before consuming the child, or roll back the new FD/backlog | **Fix:** peer-address pointer validated before the datagram/connection is consumed; late copy failure returns -EFAULT, and accept rolls back the fd and child.
| M-033 | M | open() truncates file size to 32-bit so fstat() returns a wrong st_size. vibeos_fs_lookup() returns 64-bit node.size but open() casts it to uint32_t into hw_fd_t::size (which is uint32_t); fstat() then passes f->size to hw_write_stat(), so a file >4GiB reads st_size mod 2^32. Reachable by open()+fstat on a fs declaring a >4GiB file. Distinct from M-018 (that fixed the by-name newfstatat path; here it is lost earlier, in the FD state) | fixed | kernel/arch/x86_64/arch_hw_internal.h: hw_fd_t.size uint32_t ~143-146; arch_hw.c hw_sys_open size=(uint32_t)node.size ~6220-6225, f->size=size ~6231-6233; hw_sys_fstat passes f->size ~8413-8417. Fix: make hw_fd_t::size (and pos) uint64_t and propagate the full size; widen lseek/pos too or read/lseek still truncate. Test: node >4GiB, open()->fstat() returns exact size | **Fix:** hw_fd_t size/pos are uint64_t; open, read and lseek carry the full width.
| M-034 | M | kernel entry point not validated against loaded segments. The ELF parser copies e_entry straight into entry_point (PE: image_base+entry_rva); uefi_validate_segment_layout() checks segment size/range/overlap but NOT that entry_point falls inside a loaded, executable PT_LOAD/section. uefi_boot_handoff() then jumps there after ExitBootServices. A crafted/corrupt image with valid segments but e_entry outside them jumps to an arbitrary/unmapped address -> immediate crash instead of a clean reject. Reachable by replacing the kernel ELF on the ESP | fixed | boot/bootloader_core.c: ELF entry ~477, PE entry ~574-575; boot/uefi_pe_loader.c validate ~13-48 (no entry check), propagate ~75; boot/uefi_boot_handoff.c call ~142-148. Fix: after parsing, require entry_point canonical and fully inside a loadable EXEC segment (PE: checked image_base+entry_rva); reject before AllocatePages/ExitBootServices | **Fix:** ELF and PE planners require entry inside an EXEC segment; red cases added to `test_bootloader_firmware_tags_and_pe_plan`.
| M-035 | M | a KERNEL task does not preempt a running NORMAL one before its quantum ends: `hw_higher_class_runnable()` only tested `mine == IDLE`, while the policy orders KERNEL > NORMAL > IDLE and says a higher class always prevails | fixed (verified against the code: the loop compared only for an IDLE current task) | the rule moved into `vibeos_sched_policy_should_preempt()` in sched_policy.c - candidate class < current class, so a class added later needs no special case - and the arch function only builds the runnable mask and calls it. Host test in sched_policy_tests.c (KERNEL over NORMAL, NORMAL over IDLE, nothing over KERNEL, no preemption within a class). **Found while verifying, and corrected in the same change:** the reviewer says the adopted kernel task is a KERNEL-class task; it is not - `hw_task_adopt_kernel` never admits it to the policy, and no task anywhere is admitted as KERNEL, so today the fix has no observable effect in a boot (the defect is real in the code, latent in the system). My first version treated an unadmitted slot as "unknown, so preempt", which made the kernel task lose the core every tick and turned the boot gate red with `quantum_not_spent` - now it is NORMAL, as `vibeos_sched_policy_class` already says, with a test. **The arch wiring itself is not host-testable** (`hw_higher_class_runnable` is static in arch_hw.c); it is covered by the boot |
| M-036 | M | PROT_EXEC was not enforced: no user leaf ever carried NX, so every user page - stack, heap, a PROT_READ mapping - was executable and W^X was a property of the API only | fixed (verified, and **wider than reported**: the arch layer builds most user leaves itself - ELF segments, stack, brk, mmap - not only through `vibeos_vmspace_leaf_flags`) | NX on every user leaf unless executable is granted: `leaf_flags`, `vibeos_vmspace_protect` (both directions), ELF segments (X from p_flags), stack, brk, mmap and mprotect (PROT_EXEC now passed through). **Three places rebuilt an entry and would have dropped the bit silently**, each fixed and tested: the clone for fork, both copy-on-write installs, and swap-out/in (NX rides in the not-present entry). `EFER.NXE` is enabled on every core in `hw_enable_syscall` (the AP trampoline sets only LME), with a CPUID check. Reported by pageinfo (`VIBEOS_PAGE_NX`) and asserted from ring 3 in the ABI self-test: RW page has NX, +EXEC clears it, -EXEC sets it again. Host tests: leaf/protect/clone/COW/sole-owner in vmspace_tests.c. Not covered: an actual instruction fetch from a data page raising #PF |
| M-037 | M | `st_blocks` = `(size + 511) / 512` wraps for a size within 511 of 2^64, giving 0 | fixed (verified) | `vibeos_ceil_div_u64()` in include/vibeos/ceildiv.h (`n / u + (n % u != 0)`), used by `hw_write_stat`. Host test at UINT64_MAX and UINT64_MAX-510, red with the old form. The other `+ 4095) / 4096` sums in arch_hw.c are boot-time sizes from the memory map, or lengths M-006 already bounds - not touched. **Re-reported 2026-09-24** against `arch_hw.c ~8400`, where hw_write_stat was before the syscall handlers moved to `kernel/abi/linux/fs.c`; the fix and its test were in place. What was missing was the sabotage: `core-ceildiv.txt` puts `(n + unit - 1) / unit` back and `test_ceil_div_no_wrap` goes red |
| M-038 | H | `pageinfo` read `*(uint64_t *)phys` from a frame it did not own: a sibling's munmap frees it, another process is handed it, and first_word returns that process's bytes (cross-process disclosure) | fixed (verified; **High rather than Medium** - it is a disclosure reachable from ring 3 with no flag) | new `vibeos_frame_try_get()` (a reference only if the frame already has an owner - it will not resurrect a free one). pageinfo pins the frame, re-reads the PTE and proceeds only if it still names the same frame, and takes frame identity, owner count and first_word from that one pinned instant; if the page is gone it reports flags only. The output copy also moved to the fault-tolerant `vibeos_uaccess_copy` (the kernel wrote `out_uptr` directly). Host test for try_get, red if it resurrects a free frame. **The race itself is not reproduced**: the window is a few instructions between two cores |
| M-039 | M | `ntfs_index_scan` read `$INDEX_ROOT`'s `value_off`/`value_len` straight from the attribute header and used them unchecked to build `root` and bound `ntfs_index_walk` - the H-011 defect (`ntfs_data_attr`, $DATA) never applied to the directory path. `rec` is a stack array in every caller (`ntfs_op_lookup`, `ntfs_op_list`), so a crafted directory record reads past it: a stack disclosure or crash reachable from an ordinary lookup or listing on a mounted volume, not a privileged path | fixed (verified; external report) | `ntfs_index_scan` in `kernel/fs/ntfs.c`, mirrors `ntfs_data_attr`'s bound: `attr_len` recomputed from the attribute (find_attr's own check, not trusted secondhand), `value_off` required in `[0x18, attr_len]`, `value_len` required `<= attr_len - value_off`. First proof was a genuinely out-of-bounds `value_off` (0xFFF0): AddressSanitizer aborted on `stack-use-after-return` in `rd32`, called from a frame that had already returned (`ext2_map_block`'s) - real, but not deterministic (a 16-bit offset may or may not clear an 8 MiB host stack depending on call depth; the same input passed clean on a later run with the defect still in place). The gate is `test_ntfs_index_root_wrap` now: a second, well-formed index blob planted elsewhere in the *same* record (still in bounds) that a corrupted `value_off` points at instead of the real one - the unfixed driver lists the decoy's name, "HACKED", proving the cross-attribute read without needing UB to be caught in the act. `cases/fs-ntfs-index-root.txt`, red on that assertion. Green under a plain host build, gcc, clang, 3 boots and ASan |
| M-040 | M | `hw_sys_write`'s small-file buffering loop dereferenced the user pointer directly (`f->wbuf[f->wlen++] = (uint8_t)p[i2]`) instead of the fault-safe `vibeos_uaccess_copy` every other buffered path in the same file uses - the dispatcher's row validates the range once, before the handler runs, but that check and this read are two instants (H-010's family): a sibling thread of the same process can `munmap` the buffer in between and the kernel takes the ring-0 fault instead of returning EFAULT. Reachable by any program with a second thread and no privilege beyond that; survived C4's move of the range check into the dispatcher, which changed where the range is validated but not that this read still trusted the validation to still hold | fixed (verified; external report) | `hw_sys_write` in `kernel/abi/linux/fs.c`: the loop copies through `vibeos_uaccess_copy` into the free space in `f->wbuf`, returning -EFAULT if the very first copy of the call faults (matching this file's convention elsewhere: `total > 0 ? total : -VIBEOS_EFAULT`, here 0 bytes are ever committed before the copy either way, so the two are the same). **The race itself is not reproduced**: it needs a second thread racing `munmap` against the syscall, which none of this project's existing test programs do; the recovery mechanism itself is proven by `uaccess-recovery.txt`, and the fix is the same pattern already applied and reproduced for H-010's other five sites |
| M-041 | M | swap-in never frees the slot it read from: `vibeos_vmspace_swap_in` (vmspace.c) brings a page's contents back and installs a present PTE, but the layer beneath it (swapmap.c) explicitly must not be depended on by vmspace.c (anon.c's own comment on the eviction side: it "is the layer below this and must not depend on it"), so nothing inside vmspace.c returns the slot - and nothing above it did either. `hw_handle_cow_fault` (arch_hw.c), the only real caller, discarded the slot number it read via `vibeos_vmspace_swap_slot` and never called `vibeos_swap_free`. Every page a process ever faulted back in permanently retired one swap slot; the eviction side (anon.c) already frees correctly on its own failure path, which is why only the bring-back side leaked | fixed (verified; external report) | `hw_handle_cow_fault` in `kernel/arch/x86_64/arch_hw.c`: the slot number `vibeos_vmspace_swap_slot` already returns is kept, and `vibeos_swap_free(slot)` is called once `vibeos_vmspace_swap_in` succeeds - the only place that knows both that the bring-back worked and which slot it emptied. **Not independently reproduced**: the leak needs sustained memory pressure to trigger repeated swap-out/swap-in over one boot, which no existing test drives, and the caller is architecture-only code with no host-test path; `vibeos_swap_alloc`/`vibeos_swap_free`'s own round-trip is already covered by the boot-time swap self-test (`io_bringup.c`) and by `vmspace_tests.c`'s host coverage of swap_out/swap_in themselves, neither of which exercised the missing call |
| M-042 | M | Code scanning #124-126 (cpp/overflowing-snprintf, reported high): the C6 diagnostics torture (`tests/kernel/diag_torture.c`) built its expected lines with `n += snprintf(buf + n, cap - n, ...)`. snprintf returns the length it would have written, so after one truncation `n` exceeds `cap` and the next `cap - n` wraps to a huge size_t - a write past the buffer. Test code, and every expected line fit its buffer with room to spare, so it never fired; nothing prevented the next longer dump from overwriting the torture's own stack | fixed (verified; code scanning) | One bounded `app()` helper (vsnprintf, `format(printf)` attribute kept) replaces all six such calls, including three the scanner had not flagged; a model text that does not fit fails the torture instead of being cut, because a cut model compares the wrong string. `core_c6_diag.md` |
| M-043 | M | Code scanning #120-122 (cpp/stack-address-escape): `vibeos_abi_linux_register` keeps the caller's row pointer for the life of the registry, and the host ABI test handed it arrays on the stack, leaving the registry pointing into a dead frame until the next `vibeos_abi_linux_reset`. Nothing read it in that window; the first test to query the registry without resetting would have read whatever the stack held. The kernel's own caller (`dispatch.c`) passes static tables | fixed (verified; code scanning) | The test's tables are `static`; `abi.h` states the contract (rows must outlive the registry). Not reproduced as a failure - there was no reader in the window - which is why the fix is the contract, written where the next caller reads it |
| M-044 | L | Code scanning #123 (cpp/declaration-hides-variable, note): in `ntfs_index_scan` an inner `attr_len` ($INDEX_ALLOCATION's length) shadowed the outer one ($INDEX_ROOT's, added by M-039). Correct today; in a parser of an external format, two attributes' lengths under one name is how one bound gets checked against the other | fixed (code scanning) | Inner variable renamed `alloc_len`, with the reason beside it |
| M-045 | L | Code scanning #118 (cpp/loop-variable-changed): `tcp_abort_socket` (`kernel/net/inet.c`) shifted a listener's backlog with the search loop's own counter, correct only because a `break` followed. Sabotaging the removal (leave the child queued; do not close the gap) went NOT RED against the host suite, and the reason is structural: a child joins a backlog only on becoming ESTABLISHED, and the routine is only called for SYN_RECEIVED children (H-030) and orphans (M-023), neither queued - the loop is unreachable today | fixed (code scanning); unreachable code recorded | Search and shift use separate counters. The unreachability is written beside the loop, with the two sabotages that would become runnable once a caller aborts a queued child; no case file, because an empty one would make `check-subsystem.py` count `net/inet` as sabotage-covered |
| M-046 | L | Code scanning #111, #112, #117, #119: an unused local and a comparison made dead by the line above it in `sched_torture.c`; `irq_handler` in `kernel_tests.c`, orphaned when `kernel/core/interrupts.c` was removed; an inner `k` in `hw_sys_execve` (`kernel/abi/linux/proc.c`) shadowing the function's own `k` | fixed (code scanning) | Removed, removed, removed, and the inner counter renamed `ci` (17 lines, every one only the rename) |
| M-047 | H | NTFS reads had no volume bound at all: exFAT and ISO9660 were bounded by their partition in H-029, and `vibeos_ntfs_t` did not even have a field for it, so a run list pointing past the partition read the neighbouring partition's sectors through an ordinary `open()` and `read()` (the block cache bounds by the whole disk) | fixed (verified; external report) | `ntfs_read_sectors`, the one funnel every NTFS read goes through, is held to the tighter of two limits: the partition table's (`part_sectors`, set by `storage.c` as for exFAT/ISO) and the size the volume declares for itself (boot sector 0x28, plus the backup boot sector), which also bounds a volume mounted with no table. Checked as `count > limit - sector`, never as a sum. Red first: `test_ntfs_read_bounded_by_volume` (cluster 100 refused, cluster 50 still served, for each limit). `fs-ntfs-bounds.txt` cases 1-2 |
| M-048 | H | NTFS directory index walker: every bound was `off + n > len` with `off` read from the index header - a first-entry offset within 16 of 2^32 wraps the sum, passes the check, and the entry is read from four gigabytes past the node. The report placed it in the `$INDEX_ALLOCATION` branch as M-039's twin; verified, the defect is in `ntfs_index_walk`, which serves **both** the resident root and every INDX block - M-039 bounded the root's value, not the offsets inside it | fixed (verified; external report, location refined) | Every bound rewritten as `n > len - off` after `off <= len`; an impossible first offset is a malformed node (counted, `ntfs_bad_metadata`), not an empty one. `test_ntfs_index_offset_wrap`: **red by crashing the suite** - the only offsets that wrap point 4 GiB away, so there is no in-bounds discriminator on a 64-bit host (the M-039 lesson, applied: said rather than dressed up as an assertion). `fs-ntfs-bounds.txt` cases 3-4 |
| M-049 | H | ext2 `readdir` (`ext2_op_list`) copied `name_len` bytes with no bound at all - `lookup` checked the block - so a name_len larger than its record copied the next records and, at the end of a block, up to 254 bytes of the kernel stack array past it into an unprivileged `readdir()` | fixed (verified; external report) | Both paths bound the name by its own record (`name_len <= rec_len - 8`), which also stops lookup's comparison running into the next record. Red first with an in-bounds discriminator: an 8-character name in a 16-byte record with name_len 12 made the unfixed driver return the next record's inode bytes as a ninth character, deterministically. `fs-ext2-names.txt` |
| M-050 | H | `net.c` socket address helpers (bind/connect/sendto read; accept/recvfrom write) dereferenced the raw user pointer. The dispatcher's range check and the access are two instants - in accept/recvfrom separated by a blocking wait - so a sibling's `munmap` made the kernel fault in ring 0 and panic (H-010's family). **Also found, not in the report**: `netctl` ops 0 and 3 wrote the user struct directly while holding `g_net_lock` | fixed (verified; external report + sweep) | Both helpers copy through `vibeos_uaccess_copy` via a local; accept's undo path, written for a failure that could not happen, now can run. netctl gathers under the lock and copies out after releasing it. **The race is not reproduced** (it needs a thread racing munmap against a blocking accept); the mechanism is gated instead - see M-052 |
| M-051 | H | `read()` on a regular file handed the user's buffer straight to the filesystem, which copies into it with plain memcpy - the same race-to-panic | fixed (verified; external report) | Read through a one-page kernel buffer (512 bytes on the stack if no page is free) and copied out with `vibeos_uaccess_copy`. A page rather than a small chunk because the FAT reader walks the cluster chain from the start on every call - small chunks would make a large read quadratic - and a kernel stack is 8 KiB or less. Not reproduced; gated by M-052's check |
| M-052 | H | `write()` to stdout/stderr read the user's buffer directly - M-040 had fixed only the file branch - and did so under the console lock; the NUL detector in the same branch also read it directly. The report's deadlock half does not hold: the console lock is recursive per core, so the panic path on the faulting core re-takes it; the panic is real. **The sweep found four more of the same shape**: `getdents64`, `uname`, `prlimit` (and M-050's netctl). Eight sites in one review | fixed (verified; external report + sweep) | The console branch copies in 128-byte chunks inside the one critical section - safe, because a faulting copy resumes at its recovery point before the trap handler prints anything; the detector reads through copies too; getdents64/uname/prlimit build in the kernel and copy out once. **New check, `check-user-access.py`, in `check.sh`**: in `kernel/abi/linux`, every integer-to-pointer cast must be an argument of `vibeos_uaccess_copy`, `hw_copy_user_string` or the pipe module (which copies through that callback); two reasoned exceptions (pageinfo reads kernel frames). It reports 11 violations at the parent commit and 0 now; `core-user-access.txt` puts three of the shapes back and it goes red each time |
| M-053 | M | GUI canary written with 8-byte stores at `back + width*height*4`: on a screen with an odd pixel count that is a 4-byte boundary. Found by CI (clang Debug, UBSan: "store to misaligned address") on the first push of C7 step 4; gcc's UBSan in the nightly passed the same code | fixed (verified) | The canary starts at the pixel count rounded up to even (`VIBEOS_GUI_GUARD_PIXEL`), the buffer size is one macro (`VIBEOS_GUI_BACK_BYTES`), and init refuses a buffer that is not 8-byte aligned, by name. The host test's screen is 201x151 so the odd case runs on every build. `io-gui-ubsan.txt` puts the old placement back and the CI sanitizer configuration (`verify-host-sanitized.sh`) reports the same error |
| M-054 | L | `check-exec-layering.sh` exited 0 when the function it guards was gone - the one failure path left after the others were fixed to exit 1, so an exit-code reader (sabotage.py's verifier) saw a pass | fixed (verified; external report) | `exit 1`. `core-exec-layering.txt` case 2 renames the function: NOT RED against the old script, red against the fixed one |
| M-055 | L | `.f.sh` printed and asserted a list of checks that left out `task-identity` and `exec-layering` - and `user-access`, `net-stable` and `fuzz-rc`, which came later | fixed (external report) | All asserted; the comment above the loop already said "every check it prints, it asserts" |
| M-056 | H | `vibeos_vmspace_swap_out` had no final re-check (external report, one line). Reading the code found it wider: reclaim runs from the allocation path on any core and evicts from another process holding none of its locks, so (1) the owner exiting freed the page tables being walked and CASed into, (2) munmap freed a slot still being written, (3) a fault read a slot not yet written - the entry said "swapped" before the write. The anonymous tier's hand and counters were unlocked too | fixed (host-verified; external report) | Claims in the rmap keep the victim's page tables alive (teardown waits in `forget_root`, bounded by a named panic); the page stays present and read-only-marked (`VIBEOS_PTE_SWAPOUT`) while written, a store cancels, and one final exchange commits - fork, munmap, mprotect and compaction all make it fail. Honoured by the fault handler, `hw_user_range_ok` and every entry rebuild. First host test of anon.c; 11 new sabotage cases all red by name. **No run on a machine has ever reclaimed an anonymous page** - the soak included (`anon_scanned=0` at 12,000 rounds) - so the machine half is proved by review and host tests only. See `mm_swapout_protocol.md` |
| M-057 | H | `vibeos_cache_get` handed out a page-cache frame with no reference and the cache lock already released; the loader mapped it and the exec reader copied from it. An eviction on another core in between frees the frame, and the mapping then takes its reference on a page already handed to somebody else - whose exit later frees it | fixed (internal) | `vibeos_cache_get_ref` takes a frame reference under the cache lock; all three users give it back after copying or after the mapping holds its own. Host test: a referenced page survives an eviction and goes on the last put; `mm-cache.txt` removes the reference and it goes red |
| M-058 | M | The clean reclaim tier never reported a frame freed. It told an eviction from an empty slot by looking at the entry - and `cache_evict` empties an entry it evicts, so a page just given back looked like a free slot: the loop stopped at its first successful eviction and counted it as nothing (`freed_clean=0` in every log there is) | fixed (internal) | `cache_evict` says whether it freed a frame; empty slots are skipped with a bound. Found by the first test to ask reclaim for a page; `mm-cache.txt` puts the old judgement back and it goes red |
| M-059 | L | The free-page poison watch printed under the frame layer's lock, taking the console lock inside it | fixed (internal) | Records under the lock; the allocating core prints after leaving the layer. Arch code with no host test - reasoned from CLAUDE.md's earlier deadlock of the same shape, not demonstrated |
| M-060 | H | Under svc-press's allocation load (28 MiB, far above the watermarks) the machine stops: cores fault in ring 0 at garbage addresses while inside the timer interrupt on its IST. Cause: the loader handed over the memory map it took *before* loading the kernel, so the kernel image was in the frame pool, and the allocator reached the page holding the IDT and the timer's IST | fixed (internal) | The loader refreshes the map after its last allocation (`vibeos_bootloader_refresh_memory_map`); the kernel reserves its own image regardless and prints `in_free_map=`, which the gate asserts is 0. Mechanism shown both ways with svc-press: both halves removed wedges at 112 blocks, the old signature; the reservation alone survives to 256 with the gate red on `kernel_image_in_free_map`. Cases `boot-memory-map.txt` (boot) and `boot-memory-map-core.txt` (host). Write-up in `mm_reclaim.md` |
| M-061 | M | A munmap burst overflows the TLB quarantine's 512 slots, and an overflowing frame is leaked by design (H-015) - svc-press unmapping 64 MiB loses 2,486 frames, ~10 MiB, for the rest of the boot | fixed (internal) | A frame is parked only when another core has the address space loaded (`loading_cr3`/`loaded_cr3`, published around every CR3 write); single-threaded unmaps never park. svc-press: overflow 0, 8 frames lost. svc-press now runs every boot and is gated; the old `munmap_never_deferred_a_frame` became timing-dependent (2 boots in 10) and was replaced by a kernel self-test of both cases. `mm_tlb_quarantine.md`, cases `mm-tlbq-holders.txt`, `svc-press.txt`, `services-press.txt` |

**H-015..H-020 and M-017 arrived 2026-09-14, one at a time, all verified against
the code.** Four of them (H-016, H-018-read, H-019, H-020) are the "dozens of
validate-then-use sites" `uaccess_recovery_open.md` already flagged, now being
enumerated: one fault-safe-copy pass fixes them. H-017/H-018-rip are one theme
(a ring-3-controlled RIP reaching iretq); M-017 and H-015 stand alone.

Checked by the reviewer and **not** promoted, recorded so nobody re-reports them:
the futex waiter slot is kept until its own waiter returns (the earlier ABA is
closed); `ARCH_SET_GS` is refused because `%gs` holds per-CPU kernel state; and
`hw_sys_lseek` truncates the offset to 32 bits (the finding's own second item,
withheld by its author for want of a security chain - it stays here, unpromoted,
with the other validate-then-use sites).

## Found internally while verifying the above

| What | Status | Evidence / next step |
|---|---|---|
| hw_task_exit makes `next` current with interrupts possibly on; a timer there saves the dying task's kernel frame as next's context | fixed (commit "core: exit switches tasks with interrupts off") | must-be-zero counter `exit_switch_irq_on` red at 4 per boot, 0 with `cli`; twelve boots 12/12. **It was not the cause of the four-worker crash family, or not the only one: the same signature recurred after the fix** (`.boot-evidence/fail-20260911-154006-boot7.log`, rip `0x405b72`, free-page-poison panic). The window was real and stays closed; the family is open again. [boot_repeatability.md](boot_repeatability.md) |
| THREADS four-worker crash family (new thread faults in musl `start`, stack is the free-page poison) | **open - and the probe masks it** | with probes in the kernel: 0 crashes in 70 boots; without: 2 in 36 (`probe-20260911-174620-boot1`, `-boot35`). The quiet probe logged nothing and still masked it; what it kept was a page walk and a user read in `hw_task_load_cpu_state` when a thread starts from its initial context - so the window is at a thread's first start. The gate now snapshots every core from the QEMU monitor the instant the poison marker appears - no guest-side work (commit "gate: snapshot every core at a THREADS thread-fault"). Read the snapshot on the next crash. [boot_repeatability.md](boot_repeatability.md) |
| Boot gate hung 48 minutes on a guest that had panicked | fixed (commit "gate: the serial pump returns while a guest is still printing") | the serial pump looped until the socket was empty, so a guest printing without pause - faulting in its own panic path - kept it from returning, and the deadline and silence budget, checked outside it, never ran. `drain_available` returns after 0.25 s; its self-test hangs with the budget removed |
| `rmap_mismatch=1` with `rmap_audit_torn=0` | **partly fixed, recurred - reopened** (commit "mm: fork's audit counts the references the TLB quarantine holds") | an unmap removes the holder at once and the owner only when the quarantine drains, so a still-mapped frame has one owner more than holders, with the owner count still. The audit now adds references the quarantine actually holds; any other disagreement still counts. Host test red first. **But `rmap_mismatch=1` with `torn=0` recurred on a kernel that includes this fix** (`probe-20260911-200632-boot9`), so the quarantine was a real partial cause and not the whole one. Open. Next: when the audit finds a mismatch, log the frame and its holders, owners and quarantined count, so the disagreement is described rather than only counted |
| The capability/MAC engine (`kernel/core/policy.c`, `kernel/core/security.c`) is called only by `vibeos_kmain`'s own init (`vibeos_policy_init`) and by `kernel/proc/process.c`'s token get/set - **never from the real syscall path** | **open, and not promoted to a fixed row on purpose** | Verified: `grep`ing `vibeos_policy_\|vibeos_security_` against `kernel/abi/linux/*.c` and `kernel/arch/x86_64/*.c` (the real Linux ABI, the only path that serves a program) returns nothing. This is the architectural fact already on record - `vibeos-arch-reality` notes networking/security "still host models" as of the portable-kernel bring-up, and CLAUDE.md's own "BOOT_OK is printed after userland has finished" section says `vibeos_kmain` and the portable subsystems it owns run *after* the arch layer has already done all its work, "which is also why it can be host-tested and yet have no runtime role." `process.c`/`policy.c`/`security.c` are that portable model: a capability/MAC state machine with host tests, never consulted by `hw_sys_*`. Wiring real enforcement into every live syscall is a security-critical feature, not a bug fix - the scope of a phase, not a line. **Left open rather than rushed**: recorded here so the gap is legible instead of silently believed closed, and picked up as its own phase when the plan reaches it |

## Order of work

Incoming findings first - the user's stated priority - and the core plan is not
dropped:

0. H-011 is done, and its audit found the read-path row above - to be fixed with
   M-009 and M-010 as one filesystem-bounds change.
0b. H-012 is done.
1. The network findings H-008, M-007 and H-009 are done, and so is the exit-window fix - which did not end the four-worker crash family; that family is open again.
2. H-010 + H-003 are done - and with them C5's "real exception table" item.
3. Every review finding is closed. What remains open is internal: the four-worker crash family.
4. Core plan C5, remaining: fork not atomic against its own threads
   (address-space lock); descriptors per thread; a must-be-zero check for
   `hw_procstate_t`.
5. The two open tooling/defect rows above.
