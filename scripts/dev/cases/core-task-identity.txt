# C5: the task identity has one definition, and the reset clears all of it.
#
# 1. hw_task_t declares an identity field again: the compiler accepts two unrelated
#    members called pid; check-task-identity.py does not.
# (the reset itself is core-task-identity-reset.txt: it is proved by the host test.)
#
# Target source: kernel/arch/x86_64/arch_hw_internal.h
# Run: SABOTAGE_VERIFY="python3 scripts/dev/check-task-identity.py" python3 scripts/dev/sabotage.py kernel/arch/x86_64/arch_hw_internal.h scripts/dev/cases/core-task-identity.txt

### hw_task_t grows a second pid
--- old
    vibeos_task_t id;
+++ new
    vibeos_task_t id;
    uint32_t pid;
