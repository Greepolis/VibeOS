# Target source: kernel/arch/x86_64/arch_hw.c

### a ring-3 fault halts the machine instead of killing the task
--- old
    if ((frame->cs & 3u) == 3u) {
        hw_fault_kill_current_user(frame, fault_address);  /* no return, if it can */
    }
+++ new

### the fault is swallowed and the task keeps running on a bad state
--- old
    g_tasks[g_current_task].exit_signal = sig;
    hw_task_exit(128ull + sig);   /* switches away; does not return */
+++ new
    (void)sig;
    return;
