# Target source: user/prog/init.c
#
# svc-press is started from init's manifest, and the gate asserts it ran -
# "a test binary ships whether or not anything runs it" (CLAUDE.md). Run with
# SABOTAGE_VERIFY=scripts/dev/verify-boot.sh. Expect press_never_ran.

### init no longer starts svc-press
--- old
    {"svc-press", "EFI/BOOT/SVC_PRES.ELF", 0, 0, 0, 0, -1, SVC_STOPPED},
+++ new

